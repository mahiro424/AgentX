# AgentX 领域上下文

AgentX 是独立开发的桌面 AI 工作台。本文统一已确认的产品概念与授权语义，不代表对应能力已经实现或验证。

## 领域语言

**通用工作台 (Workbench)**：
用户以自然语言提出最终目标，由 AgentX 理解现有材料、规划步骤并在授权范围内执行连贯工作，用户检查过程与结果后继续调整。工作涵盖本地代码项目、办公文档及跨软件协作，不以预先列出的软件名单定义产品边界，但具体操作受可用能力和实际授权限制。

**任务 (Task)**：
用户围绕一个工作目标与 AgentX 持续协作的工作单元，包含相关材料、执行过程、工作结果和后续调整。界面称为“会话”，并非另一层独立对象；同一会话可以包含多轮执行。

**授权范围 (Authorized Scope)**：
任务实际被允许访问的文件、网络资源及执行的软件操作范围。用户提出工作目标或选择一个模式，不代表尚未取得的系统、应用或外部服务权限已被授予。

**授权模式 (Permission Mode)**：
任务采用的授权边界与审批方式组合，包括请求批准、帮我批准和完全访问权限三档。每个任务可单独选择，界面应反映实际生效的模式，不可将不可用的模式显示为已生效。

**请求批准 (Ask for Approval)**：
新任务的默认授权模式，允许在已授权范围内自主工作，需要额外授权的操作交由用户审批。它不表示每一步普通操作都必须询问用户。

**帮我批准 (Approve for Me)**：
保持请求批准模式的授权边界，将符合自动审查条件的额外权限请求交给自动审查，而不是直接放行全部操作。未获批准的操作不执行，必要时寻找替代方案或停下来交由用户处理；不具备自动审查能力时，该模式应明确显示为不可用。

**完全访问权限 (Full Access)**：
由用户显式选择的广泛授权模式，放开常规文件与网络执行限制，不再进行这类操作的逐项执行审批。它不新增软件操作能力，不等同于系统管理员身份，也不替代操作系统、应用或外部工具自身的授权。

**工作结果 (Work Result)**：
任务交付给用户检查的产出与执行事实，包括产出文件、既有内容的改动、执行记录，以及已知失败或未完成事项。任何授权模式下都应保留可检查的过程与结果，不得将未验证结果描述为已验证成功。

**结果检查 (Result Inspection)**：
用户检查工作过程、全部改动与结果，并通过自然语言继续提出调整要求。它是协作行为，不是必须打勾的产品状态，不建立“验收通过／要求修改”记录或门禁。执行结束不等于用户目标已满足。
