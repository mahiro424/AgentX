# M2-R1 完整工作链路分期核验

日期：2026-09-09。正式工单 [#30](https://github.com/mahiro424/AgentX/issues/30)。本次只修改已批准的产品规格、状态分期与文档门禁，不实现新的材料或办公功能，不变更既定 UI、Codex、依赖或权限。

## 实际基线

- M2-01 的 [PR #29](https://github.com/mahiro424/AgentX/pull/29) 已合入 m2，合并点 `50c1f9c1b61bbb0a8a6243fa903aef7164d800e2`；#21 已关闭。
- 当前搜索切片 head `58caab92f40b1e1e17de152c5a64a2d0fafa2840` 的 [Windows PR 检查](https://github.com/mahiro424/AgentX/actions/runs/34323887970)为 **235/235**（140803.5033 ms）；本地完整回归 **235/235**（337885.5646 ms），0 fail/cancelled/skipped。原 PR 检查失败和本地诊断记录仍在原验证文档，不覆盖旧结果。
- 现有可运行包未因文档修订改变，app.asar SHA-256 `029e4320ec21113d399b0f78e9248015778ade8d6781e31a5342c2245272813c`。本次不以该包冒称材料/办公功能已经可用。

## RED 与 GREEN

1. 新增分期合同回归，要求 M2-02 有文本生成、独立核对、预览及续改，M2-03 有表格预览/续改，M2-04 有文档读取/续改。在原 PRD 上明确失败：`纵向交付缺少 M2-02/generatingText`。
2. 同步 PRD 的完整矩阵与检查脚本：M2 为 48 条、M1 仍为 72 条，不删除历史 M1 场景或放宽缺失检测。
3. 通过真实文档检查入口验证有效文档，再注入六种规格变异：缺定位词、缺搜索状态、缺文本预览、缺文本续改、表格预览错挂切片、缺文档读取；均必须被门禁拒绝。变异只在内存读取边界发生，不改文件凑通过。

## 分期与边界核对

- M2-02：非项目、结构化材料引用、TXT/Markdown 实际文件到预览续改；不把选择成功称为模型已经读取。
- M2-03：CSV/XLSX 值/公式处理与验证、工作表预览、续改；不把未重算缓存当新结果。
- M2-04：DOCX/文本型 PDF 的实际内容到文档产物、预览续改；不新增 PDF 生成、OCR 或专业排版。
- 材料和预览是正交产品维度，不修改引擎终态；文件版本/任务/轮次来源明确，旧响应不覆盖新版本。
- M2-05 异常恢复与所属残留收尾原范围保留；Codex 仍为唯一执行循环，master 不变。

本地文档和变异检查通过不等于本工单闭环：仍需对应提交审查/CI、PR 合入 m2、同步父 PRD 与子工单并核对依赖，之后才可将 #22 转为 ready-for-agent。正式完成证据保留在本工单及其 PR。
