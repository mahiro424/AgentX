# M2-01 会话组织与搜索：分行为验证记录

日期：2026-09-09。工单 [#21](https://github.com/mahiro424/AgentX/issues/21)，分支 m2-01，基于 m2 的 6b4db5f。本文记录逐行为交付，不表示整个 M2-01 或 M2 已完成。PR 目标仅 m2；master 不变。

## 会话改名

已接通右键、Shift+F10 与工作台标题菜单的同一编辑动作。名称由 Main 校验并以组织修订 CAS 写入；产品标题不被旧执行快照覆盖。改名不改目录、thread/turn、执行状态、观察或最近活动时间，不触发模型或引擎执行。操作 ID 用于请求标识；同一旧修订重复请求被拒绝，不承诺响应丢失后的自动重试或幂等返回。

数据库迁移到 v10，为任务增加 organization_revision，原字段与执行 DTO 保留。升级前使用 SQLite 一致性备份；失败不清库、不容忍版本与结构不一致。修订不进入引擎并发控制，Codex 仍为固定 0.153.4、零补丁。

### 实际失败及修正

1. 首次改名行为 RED：旧包没有“重命名会话”菜单；接通后同一真实 Electron 测试通过。
2. 相关回归曾 22/31 通过。9 个失败分为旧备份文件名断言、模拟旧库未移除新列，以及预期结果缺少新增组织修订。修复夹具和精确预期后，原用例及 v7 迁移回归合跑 34/34 通过。未放宽备份唯一性、旧版本、原始数据、完整性和执行关联检查，未修改产品迁移以吞掉重复列错误。
3. 过长名称 RED：502 字符粘贴被 maxlength 静默截为 500。去除截断，保留完整输入并由 Main 拒绝保存；同一实际界面测试通过。
4. 首次完整回归 192/193，唯一失败为产品桥精确白名单未增加 renameTask。只补充这一有限接口，保留桥冻结、无 Node 访问和非法偏好测试；定向复测通过。后续完整回归结果见下表。
5. 首次视觉脚本在窄窗遇到侧栏卸载：脚本在响应式收起完成前检查 isVisible，误跳过展开动作。改为等待实际“展开侧栏”入口后操作，四个真实窗口均通过。没有修改产品响应式逻辑或用强制点击掩盖问题。

### 验证证据

| 项目 | 结果 |
| --- | --- |
| 改名主路径 | 右键保存、重开持久；逐字段对照原执行事实 |
| 冲突及键盘 | 旧修订失败保留输入；Escape 返回触发处；标题菜单重开后可显式保存 |
| 名称与 IPC | 空白、控制字符、超长、非法 ID/修订、额外字段均拒绝；重复旧修订不再次写入 |
| IME | 组合输入 Enter/Escape 不保存或关闭；结束组合后可取消并恢复焦点 |
| 真实写入失败 | 独立合成数据库持有写锁，保存失败保留输入；释放后显式保存成功 |
| 升级 | 打包应用 v9→v10；备份唯一、原版本/结构/数据完整，首个组织修订从零开始 |
| 实时投影 | 原执行 owner 持有旧标题时，getWorkspace 仍返回产品新标题和原执行事实 |
| 类型与打包 | npm run typecheck、npm run package 通过 |
| 完整回归 | 194/194 通过；0 fail/0 cancelled/0 skipped；235478.2537 ms |
| 真实模型 | 本改名验证无需模型请求，使用合成数据；尚未完成 M2-AC-01 的真实历史搜索验收 |

本地原始日志位于 .local-validation/m2-01，未上传运行目录或凭据。01–03 为原始改名 RED/打包/GREEN；04/05 为相关回归；06–09 为冲突与过长输入；10/11 为首次完整回归与白名单复测；12/14 为视觉原始失败与修正后结果；13 为真实写锁；15 为完整复跑。

### 实机设计检查

使用已确认外框、侧栏操作采用图和 DESIGN 原语。没有新增管理页、任务总览或假搜索入口。逐张检查下列真实 Electron 图：菜单可读；编辑框、说明、焦点与主操作在四种窗口中完整可见；复用深浅色 Token，窄窗侧栏按需覆盖。

| 窗口 | 菜单 | 编辑 |
| --- | --- | --- |
| 浅色 1280×820 | [菜单](images/m2-01/rename-menu-light-1280.png) | [编辑](images/m2-01/rename-editor-light-1280.png) |
| 浅色 960×640 | [菜单](images/m2-01/rename-menu-light-960.png) | [编辑](images/m2-01/rename-editor-light-960.png) |
| 深色 1280×820 | [菜单](images/m2-01/rename-menu-dark-1280.png) | [编辑](images/m2-01/rename-editor-dark-1280.png) |
| 深色 960×640 | [菜单](images/m2-01/rename-menu-dark-960.png) | [编辑](images/m2-01/rename-editor-dark-960.png) |

没有重新执行全系统 DPI 或人工候选窗口检查；不把组合事件测试写成人工输入法验收。

## 会话置顶与取消置顶

在改名提交 c5db748 上继续单行为 TDD。该提交的 [Windows CI](https://github.com/mahiro424/AgentX/actions/runs/34262590767) 已实际验证 194/194；[文档门禁](https://github.com/mahiro424/AgentX/actions/runs/34262590860) 也通过。它不是完整 M2-01 的 PR 或合并记录。

置顶 RED 为原包不存在“置顶会话”悬停入口。实现后真实 Electron 完成：悬停置顶、置顶区唯一展示、重开持久、键盘菜单取消、返回原项目；task/thread/turn/执行与最近活动不变。与迁移、非法 IPC、陈旧修订和原改名回归合跑 24/24 通过。

- 新增有限 setTaskPinned 接口，Main 校验字段及组织修订；置顶与改名共享 CAS，陈旧置顶不能覆盖改名，反之亦然。
- v11 增加 pinned_at，v10 的非零组织修订保留，升级前一致性备份；旧版本夹具同步移除新增列后才标记旧版本，不容忍错误 schema。
- 侧栏悬停/聚焦和菜单共用动作；置顶只改变产品组织，运行任务也可置顶。多项菜单支持上下键、Home/End 与 Escape。
- 置顶失败不会假称成功；“重读会话列表”只重新读取，不重发失败的置顶。上一操作失败提示保留至显式新操作，不把重读列表当作保存成功。
- 首次完整置顶回归 196/197：活动状态测试的旧快照缺少组织字段，已补齐契约。进一步新增悬停断言确认运行标记会被操作按钮隐藏，实际 RED 后改为始终保留运行、等待及错误状态，仅普通活动时间在悬停时让位。该变化同时以键盘聚焦验证；不删除原活动与排序测试。
- 最终完整回归 **197/197** 通过，0 fail/cancelled/skipped，290518.3285 ms；原始日志为 .local-validation/m2-01/27-pin-full-regression.log。当前类型检查及打包通过；归档与搜索尚未交付。

### 置顶实机与包体

已检查四种窗口的唯一置顶行、左侧固定标记、多项菜单及改名兼容；真实运行与等待标记另有悬停/键盘测试。没有重新宣称全系统 DPI 或人工候选窗口检查通过。

| 窗口 | 菜单 | 编辑兼容 |
| --- | --- | --- |
| 浅色 1280×820 | [菜单](images/m2-01/pin-menu-light-1280.png) | [编辑](images/m2-01/pin-editor-light-1280.png) |
| 浅色 960×640 | [菜单](images/m2-01/pin-menu-light-960.png) | [编辑](images/m2-01/pin-editor-light-960.png) |
| 深色 1280×820 | [菜单](images/m2-01/pin-menu-dark-1280.png) | [编辑](images/m2-01/pin-editor-dark-1280.png) |
| 深色 960×640 | [菜单](images/m2-01/pin-menu-dark-960.png) | [编辑](images/m2-01/pin-editor-dark-960.png) |

置顶包已更新原 out/AgentX-win32-x64，app.asar SHA-256：9c2d7ce47b2229333a3d34313a895b6d0629e1ae4a37532541f0c0fb842b9cc6。后续打包将替换包体；此值仅标识当前已验证的置顶版本。

## 会话归档与恢复

置顶提交 b58edd2 的 [Windows CI](https://github.com/mahiro424/AgentX/actions/runs/34265403974) 已核验 197/197，[文档门禁](https://github.com/mahiro424/AgentX/actions/runs/34265403821) 通过。以下归档改动是在该提交之后的新行为，不能借用置顶 CI 宣称全部完成。

### 实现边界

- v12 增加 archived_at，升级前一致性备份；组织 CAS 与执行投影分离，归档/恢复不修改最近活动、原文件或历史关联。置顶标记保留，归档隐藏唯一行，恢复回到原位置。
- 新增有限 setTaskArchived 接口，校验精确字段、UUID、布尔值和修订；归档事务同时检查状态、未结算发送意图及本任务未释放 lease。活动/停止中/待核对记录在 UI 禁用，Main 也拒绝；恢复不调用引擎。
- 已结束的自有空闲引擎先用现有终端适配器传空 item 集合，只读核对后台列表，不发终止请求。后台为空才回收空闲引擎；未知归属或有后台命令时拒绝归档，不借归档自动停止工作。
- 续轮派发前再次检查归档状态；lease 获取和续轮意图落盘分别有原子归档条件，覆盖异步握手期间归档的竞态。unconfirmed 按已批准状态契约表示已人工收尾，不等同 reconciling；仍须无未决意图/未释放进程归属，不把它写成成功。
- 头部与菜单提供显式恢复，侧栏提供撤销归档；发送保持禁用但草稿保留。归档写入已确认而列表读取失败时，只合并已确认的组织字段，不覆盖执行观测，显示“已保存，但列表尚未核对”；侧栏收起时错误仍可见。

### RED 与回归记录

1. 28：真实窗口缺少归档入口，RED；29/30：打包后归档隐藏、恢复回到置顶区、草稿和执行字段保持通过。
2. 31/32：缺少空闲引擎归档准备接缝，RED；实现后只读后台核对和回收通过。36：真实协调服务配合合成协议边界的 11 项续轮回归通过，包括有后台拒绝、归档与握手竞态；未请求真实模型。
3. 33：活动归档按钮未禁用，RED。该测试退出清理触发了真实未决任务退出确认；仅通过该合成实例的诊断连接退出测试实例，并改用已有 crashTestApp 清理合成未决状态，不把该清理算作产品正常退出验收。35/37：新包的归档/活动禁用两个真实窗口测试通过。
4. 38：相关回归 5/14，9 项失败为旧迁移夹具未去除新归档列及精确期望缺少 archivedAt。仅更新旧版本夹具与返回契约，未放宽备份/版本/关联检查；39：14/14 通过。
5. 40/44：列表读取失败且侧栏收起时缺少可见说明，实际 RED；修复后真实窗口通过。写入成功与列表刷新成功分开，不引导自动重复提交。
6. 42：3 项存储/服务回归通过，覆盖四类未决意图、已关闭根进程但后台未证实、其他任务 lease 不被归档干预、非法请求、陈旧修订、旧对象续轮与恢复不产生意图。
7. 43/45：已人工收尾的合成 unconfirmed 记录被空闲引擎检查拒绝，RED；对齐状态契约后通过。这不是 M2-05 真实崩溃恢复验收。
8. 47：首次完整回归 206/208，两个失败为早期项目迁移测试仍匹配 before-v11 备份名。仅改为本次 v12 的备份名，保留原库版本、结构、数据与一致性检查；48：原两个用例 2/2 通过。
9. 50：最终完整回归 **208/208** 通过，0 fail/cancelled/skipped，324059.645 ms。包含真实打包应用的 v11→v12 升级、唯一备份/完整性、归档重开隐藏、显式恢复与原文件不变。合成历史夹具不代替 M2-AC-01 的真实模型历史搜索。

归档包已更新原 out/AgentX-win32-x64，类型检查通过；app.asar SHA-256 为 9b91f1a34c5eb1b644d8d4f3678d754413d02bf6a38e63a1e24d3fc7d72e0145。该值只标识本行为已验证包，后续功能打包会更新。独立只读审查覆盖归档事务、运行时回收、续轮竞态和读取失败展示，未发现可确认缺陷；本地通过不代表远程 CI 已通过。

### 归档实机设计检查

49/51 为真实 Electron 的两种尺寸、深浅色检查。10 张图逐张核验菜单边界、唯一行隐藏、撤销/恢复、草稿保留和禁用发送。视觉夹具是尚未执行的合成会话，没有伪造模型历史。960 窗口展开侧栏时沿用原覆盖式布局；额外收起侧栏后检查恢复入口完整可见、真实点击可用，未改动响应式产品设计。

| 窗口 | 菜单 | 归档状态 | 收起侧栏后的恢复入口 |
| --- | --- | --- | --- |
| 浅色 1280×820 | [菜单](images/m2-01/archive-menu-light-1280.png) | [归档](images/m2-01/archive-state-light-1280.png) | 同左，无覆盖 |
| 浅色 960×640 | [菜单](images/m2-01/archive-menu-light-960.png) | [归档](images/m2-01/archive-state-light-960.png) | [恢复入口](images/m2-01/archive-restored-entry-light-960.png) |
| 深色 1280×820 | [菜单](images/m2-01/archive-menu-dark-1280.png) | [归档](images/m2-01/archive-state-dark-1280.png) | 同左，无覆盖 |
| 深色 960×640 | [菜单](images/m2-01/archive-menu-dark-960.png) | [归档](images/m2-01/archive-state-dark-960.png) | [恢复入口](images/m2-01/archive-restored-entry-dark-960.png) |

没有重做全系统 DPI 或人工输入法候选窗口验收，不把自动事件测试或此前结果冒称本次人工专项通过。

### 归档提交的远程回归与诊断

归档提交 348bbbc 的[文档门禁](https://github.com/mahiro424/AgentX/actions/runs/34269132002)通过，但[首轮 Windows CI](https://github.com/mahiro424/AgentX/actions/runs/34269132035)为 **207/208**，不能报告通过。唯一失败是旧的孤立进程核对 UI 用例等待标题 5 秒超时；归档相关用例通过。54 保存原始失败，55 原用例本地通过。

检查发现系统身份查询的实际期限为 10 秒，而该用例沿用普通控件的 5 秒等待。56 在真实 Electron 中仅延迟真实 Win32_Process 查询应答到 6 秒，稳定复现相同超时；当时界面仍显示正在核对且无错误，没有伪造 PID 结果。修正仅针对这个真实系统查询的等待期限至 15 秒，保留所有 PID 复用、不释放归属、无引擎目录、禁止发送和草稿断言，并增加等待时禁止重复核对的检查。57 的三个诊断桌面用例全部通过，实际等待 6430 ms。

原 CI 未采集超时瞬间 UI，因此不能断言该次远程失败只可能由延迟造成；后续仍须检查新的完整 CI。没有提高产品超时、跳过核对或全局放宽测试等待，诊断探针已移除。包内产品代码未变，不需要为测试期限改动替换已验证包体。

测试修正提交 7a73337 的[Windows CI](https://github.com/mahiro424/AgentX/actions/runs/34269959494)已从实际日志核验 **208/208** 通过，0 fail/cancelled/skipped；该次真实进程查询延迟回归等待 6382 ms。[文档门禁](https://github.com/mahiro424/AgentX/actions/runs/34269959381)通过。原失败记录保留，不将这次成功改写为首轮即通过。

## 搜索服务与工作台接入（开发中，尚未完成整个切片）

已重新读取父 PRD #1 完整正文及 #21 的七项必读：app-shell 保持原构图并用浮层搜索；状态策略要求失败保留查询/草稿；US-5/6/27/30 对应组织、历史、错误与键盘；本页即壳层；DESIGN §3/§5/§6 提供系统字体、浮层与列表原语；单端无需 platforms；默认 PNG 与 04-search-conversations-corrected 约束视觉，非默认态由 UI-M2-SHELL 规定。没有用本实现记录替代正式就绪工单。

最初仅为服务接缝；现已接通有限 IPC、真实搜索浮层、显式重建和核验命中定位。Main 搜索服务使用产品元数据与公开历史读取接缝，正文缓存只含可见文本和来源，不参与恢复执行。当前轮自动更新的新增实现与验证见下节；完整重建故障恢复、容量与剩余状态仍待完成，不能据部分验收关闭切片。

| 日志 | 行为与实际结果 |
| --- | --- |
| 52/53 | 缺少搜索服务导致 RED；公开历史正文建立缓存、task/thread/turn/item 命中、重开读取与组织/执行不变通过 |
| 58 | 复用同一合成历史夹具的小范围重构，原行为保持通过 |
| 59/60 | 已识别凭据可被检索，实际 RED；只在投影中脱敏后通过，SQLite 文件也不含测试秘密；不读取附件文件或隐藏字段 |
| 61/62 | 原历史缺失使重建直接失败且旧覆盖仍显示完整，RED；保存可见缺失原因、保留旧片段、显式重建恢复通过 |
| 63/64 | 未支持可见类型被误计完整覆盖，RED；区分隐藏 reasoning 与未支持可见类型后通过 |
| 66/67 | 缺少命中定位接口，RED；重新读取公开历史、精确验证项及摘要、拒绝伪造与变更来源，显式重建后可定位；5/5 服务测试通过 |

实现依据先查 Context7 的 /websites/sqlite_docs；返回材料不足以说明字面子串边界，补充核验 [SQLite 核心函数](https://sqlite.org/lang_corefunc.html)：参数化 instr/lower 用于字面包含和 ASCII 英文大小写；中文/标点不做分词。高亮坐标单独在 JS 中计算，不把 SQL 的字符偏移直接用作 UTF-16 下标。更完整的中文/英文/标点/emoji、筛选/排序及大数据量验证仍待下一循环。

搜索脱敏仅识别明确的 Key 前缀、认证/Cookie 头和具名凭据表达，不承诺能识别任意无标记秘密；任务文本仍是本地敏感数据。未调用模型、读取正式凭据或用户项目。独立只读审查提出两个疑点：旧任务观测不等于旧消息内容失效，定位必须重读原项而不是机械拒绝所有旧索引；脱敏占位符可搜只揭示已对本地用户可见的脱敏事实，不泄露原值。未据这两项推断增加额外权限模型或阻断合法历史定位；后续仍需完整接缝与实机审查。

### 搜索浮层与真实来源验证

- 68 的源服务加入完整回归后 **213/213** 通过（310468.3452 ms），当时包内尚无搜索 UI，不能将这次回归计为新浮层验证。
- 69 真实窗口缺少搜索按钮，RED；70 打包后 71 首个真实入口用例通过：图标/Ctrl+K、标题高亮、Escape 恢复原焦点、保留查询和草稿。该次清理总耗时约 92 秒，工具句柄持续存活，未重启/杀进程；最终返回成功，但延迟根因未确认。76 的同用例为 1676.9137 ms，未通过放宽产品或测试期限掩盖问题。
- 72/73 的索引并发单测失败实际是夹具复用了唯一 threadId，**不是有效的行为 RED/GREEN**。修正为两个独立 thread/turn 并保留真实来源映射后，79 的 **6/6** 通过，验证重复请求仅一次读取、处理数量、退出暂停后不启动后续历史读取、显式重新重建。原失败记录不删；此处明确保留首次 TDD 夹具失误。
- 74 真实窗口缺少重建入口，RED；75 包内接通 getSearchIndexState/rebuildSearchIndex/onSearchIndexChanged，76 的 **7/7** 搜索/原历史桌面回归通过。索引工作属于 Main，关闭浮层不将其误认为 Agent 执行；退出先暂停后续索引读取，同时立即走原执行停止链路，最终等待两者回收；不会为了索引延迟发出引擎停止。退出失败后只恢复接受新索引请求，不自动重放重建。
- 80 重复字面命中只高亮第一处，RED；81 的 **7/7** 服务回归通过，片段内中文、ASCII 大小写、标点、emoji 全部非重叠命中使用 UTF-16 范围；引号/SQL 表达式仍为参数化字面文本，不作为查询语法。
- 82 暴露已核验搜索历史在离开后再次选择时被当作新读取的缓存复用，真实 RED；修正将定位快照限定于本次会话打开；84 的搜索/原历史桌面回归 **8/8** 通过（19936.0537 ms）。没有将索引缓存当作引擎历史真相源。

手动真实来源核验使用已有 Flash 成功验收的合成两文件项目，将原生历史作为不透明夹具复制到新数据根，不复制模型配置/密钥，不读取正式数据目录，不调用新模型。77 仅复制 sessions 时公开 thread/read 缺少产品绑定轮次，产品正确报错；78 补全原生 thread_history 文件后，通过固定 0.153.4 的公开接口读取真实历史，再从真实浮层重建、搜索命令输出 `# pass 2`，点击命中后核验 task/thread/turn/item、工具自动展开及实际焦点。

78 记录：`.local-validation/m2-01/search-real-history-JGgI0y/summary.json`，来源为已有合成 Flash 验收 `a0764618-aad2-4429-9aa5-4e83b1f36835`，模型调用 0，覆盖 1/1，源命令 `call_00_WqKExXCtMuZZegqKZIWD9355`。前后产品组织/执行字段相同，原合成项目所有文件哈希相同，没有新执行。测试夹具读取的公开历史经真实引擎提供，未替换 Main 的搜索/定位/历史 IPC；原生数据库仅作为复制夹具，产品不解析它。

85 的归档命中缺少行内恢复入口，RED；86 打包后，89 的搜索/历史实机回归 **9/9** 通过（20058.2747 ms），恢复需显式点击，CAS 使用最新组织修订，只合并确认的归档字段且保留查询，没有新执行。87 暴露仅按活动时间排序遗漏了相关度；88 的服务回归 **8/8** 通过（3648.861 ms），同一任务多正文命中去重、标题优先，其次按可复现的非重叠字面次数/活动时间/taskId 排序，项目与归档过滤、仅标题/正文和空查询均检查。

以上证明了一个真实 Flash 历史的搜索定位链路及合成多记录的确定性匹配，尚不替代全部 M2-AC-01 的当前轮更新、缺失来源恢复、容量与全状态验收。采用图已重看；初始浅色截图已检查，深色/紧凑及正式截图入库仍在后续步骤。


完整回归 91 为 **218/220**（566475.1229 ms），不能报告全绿。两个失败分别为新增搜索入口改变了紧凑顶栏的 Tab 顺序，以及旧的冻结桥精确白名单未包含五个搜索接口。只更新对应明确预期：Tab 仍需依序到达搜索/新会话并保留中文组合输入检查；桥仍精确枚举，不允许多余任意能力，额外补充非法搜索参数、未知定位请求、不创建引擎目录和同 URL 外来窗口拒绝搜索/重建/定位的断言。后续定向回归与远程完整 CI 分开记录。

93 的外框、生命周期、搜索定向回归 **19/19** 通过（41778.7237 ms），包括上述两个修正后的严格断言。随后按采用图补齐结果图标、搜索框焦点和选中底色，95 类型检查与打包成功；96 搜索桌面回归 **4/4** 通过（13326.7307 ms）。这仍不是当前提交完整 CI 的结果。

97 的四窗口截图检查发现测试脚本只调用偏好保存接口，未同步 Renderer 偏好状态，截图主题不能按文件名认定。修正脚本为操作真实设置按钮并等待 document 的主题值，98 重新采集全部 8 张图；浅深主题、1280×820/960×640 的浮层边界、控件和命中项焦点均核验。长命令只保证精确工具项展开/聚焦，窄窗口内仍需正常滚动阅读，不宣称整条输出同时可见。没有修改产品主题逻辑或用截图重命名掩盖失败。

| 窗口 | 搜索浮层 | 公开历史命中定位 |
| --- | --- | --- |
| 浅色 1280×820 | [搜索](images/m2-01/search-results-light-1280.png) | [定位](images/m2-01/search-location-light-1280.png) |
| 浅色 960×640 | [搜索](images/m2-01/search-results-light-960.png) | [定位](images/m2-01/search-location-light-960.png) |
| 深色 1280×820 | [搜索](images/m2-01/search-results-dark-1280.png) | [定位](images/m2-01/search-location-dark-1280.png) |
| 深色 960×640 | [搜索](images/m2-01/search-results-dark-960.png) | [定位](images/m2-01/search-location-dark-960.png) |

95 包已更新原 out/AgentX-win32-x64，app.asar SHA-256 为 `4615b140bc69ce2d9806521cc372fc30ddcc8218f2427416cff12709a14520fc`；98 使用该包及已有 Flash 合成历史完成只读核验，没有新模型调用。本次提交是搜索基本链路的可追溯检查点，不是 M2-01 完整交付；当前轮自动更新、故障恢复、容量和剩余状态仍继续在同一切片推进。
### 自动更新与活动轮定位

搜索基本链路已提交 `2956153d551d05b12258c57a33e1e61d9c74845d`；[Windows CI](https://github.com/mahiro424/AgentX/actions/runs/34277923139) 从实际日志核验 **220/220**（134170.1912 ms），0 fail/cancelled/skipped；[文档门禁](https://github.com/mahiro424/AgentX/actions/runs/34277923124)通过。以下为该提交之后的新行为，不借用此 CI 宣称通过。

- 99：缺少活动搜索历史接口，有效 RED。新增 `readSearchHistory` 只读本实例当前绑定且仍可核验的活动轮；普通 `getTaskHistory` 的已结束门禁不放宽。读取使用固定公开 thread/read、真实用户 item ID，前后核对 thread/turn/状态，不读取意图来伪造 ID，也不恢复或开始任务。100 执行服务回归 **10/10**（11644.2045 ms），含无归属/断线拒绝和协议请求数不增加。
- 101：缺少自动补齐接缝，RED。搜索打开时补齐未覆盖/修订变化的会话；执行通知通过 1 秒合并队列更新关联任务，索引串行且只读。失败记录保留，同修订自动尝试失败后需显式重建，不无休止重试。退出取消尚未派发的定时工作并等待已开始读取；手动重建合并已有计划。102 服务回归 **9/9**（8242.3939 ms），含合并重复更新、重开查询、原组织/活动时间不变和退出取消。
- 103：后台通知使键盘选中跳回首条，真实 RED。后台查询保留既有结果顺序/所选 task 和输入焦点，用户更改查询/筛选才重新排序；保持来源重新核验。104 类型检查、打包成功。105 在打包尚未终态时误启动测试，五项应用启动失败；这是执行顺序错误，不是有效行为 RED，日志保留。等待同一个打包句柄确认成功后，106 以正确的搜索/历史用例列表回归 **10/10**（52249.7598 ms）。
- 107：活动轮命中打开后无来源焦点，真实 RED。Renderer 分开呈现旧轮历史与当前轮；公开历史提供用户消息 ID，当前执行快照继续覆盖同 ID 的可见项并追加新项，停止/审批仍走原执行链路。终态后丢弃活动定位快照，重新读已结束历史。108 类型检查与打包成功；109 搜索/历史桌面回归 **11/11**（19789.5714 ms），含实际焦点、用户消息唯一、后续消息更新和停止仍可达。

本节活动轮使用确定性的公开协议/历史及执行快照边界夹具，不能冒充真实 Flash 活动轮现场通过。此前 78/98 证明的是已有真实 Flash 历史的冷读取定位。技术检索经 Context7 `/openai/codex` 核对公开读取/轮次契约，再与本仓库固定生成的 ThreadReadParams 对照；上游 main 文档不是固定 0.153.4 活动历史与大容量行为的实测依据，仍需现场核验。

108 包继续更新原 out/AgentX-win32-x64，app.asar SHA-256 为 `a69496d8065d9d348a02939666396614e72699ce911fbf6ee2fe833c7cd72c17`。独立只读审查后点验：delta 合并不更新 observedAt；自动失败后显式重建是既定防重试语义；原停止接缝在收到权威终态及后台核对后才返回，没有据“RPC 应答立即返回”的假设改动停止功能。完整回归与新提交 CI 另行记录。

110 本地完整回归 **224/224**（357857.5854 ms），0 fail/cancelled/skipped，通过当前实际包与源码的全部无密钥测试。新增功能的远程 CI 仍须对应后续提交单独核验。

### 活动输出遗漏与真实现场复测

111 真实 Flash 现场未通过，阶段为“定位真实活动工具输出”。原摘要在清理前写入，其 `running` 不是最终状态；112 对原存活实例的公开执行/退出接口核验为 `interrupted`、无执行错误、退出处于 `confirm`。命令退出码为 -1，实际停止已生效。测试漏答退出确认导致进程仍存活；通过该隔离实例的 `answerExit` 正常收尾后，同一测试句柄返回失败终态，没有另起任务覆盖原失败或批量杀进程。

113 经执行服务、真实事件解析/存储和搜索服务复现：公开历史缺少命令，或命令尚无输出时，即使实时输出已经可见，正文搜索仍返回 0。修正仅在产品的活动搜索读取路径合并同 runtime/session/thread/turn 的已验证可见项，按真实 item ID 去重；用户项仍由公开历史提供，不伪造 ID，不改变普通已结束历史门禁。114 执行与搜索服务回归 **20/20**（13592.072 ms）。这证明了遗漏接缝，不将上游 main 文档推断当成对 111 原始活动回包的抓取证据。

115 类型检查与打包通过，app.asar SHA-256 `aaa0483b51b14b33f4da13b27500fab9dfbe9e16ee6ed6a186cf34e7d62a81b8`。116 使用该真实 Electron 包，仅调用 `deepseek-v4-flash`，一次顶层提交：

- 用户消息命中真实 `01a082f4-008c-7610-95b2-cb26427a9eda`；命令输出命中真实 `call_00_jhd7z5pUhA26RuKngGhO4019`。
- 两者属于同一 thread `01a082f3-b4e3-7d80-b2b2-e464ca59ce32` / turn `01a082f3-ffc1-7c01-9404-e19876024f0b`；定位时仍在运行，停止按钮保留。
- 随后同轮 `completed`，命令退出码 0 且收到 `PROBE_FINISHED`；探针脚本未修改、目录无额外文件、原合成档案的数据库/配置/加密凭据哈希未改变。清理快照 `completed`、无错误，产品正常退出，测试进程 exit 0。
- [活动用户项](images/m2-01/search-active-user.png)、[活动命令项](images/m2-01/search-active-command.png)为实机图，已检查焦点和停止入口。一次顶层提交不等于一次底层模型请求，不冒报模型请求总数。

### 搜索缓存归属与损坏重建

117 的目录链接/硬链接归属用例为有效 RED；增加缓存目录、SQLite 本体和 journal/wal/shm 普通文件核验。118 搜索回归 **10/10**（8833.3781 ms），外部目标字节不变，拒绝后不读取历史。不把此预检描述为同一 Windows 用户下对任意并发恶意进程的强隔离。

119 损坏 SQLite 无法显式重建，RED；120 回归 **11/11**（10943.2413 ms）。修正后仅用户显式重建会核验并保留已确认损坏的索引及 sidecar，再从公开来源重建；自动读取/补齐不清库。已保留提示与覆盖状态可见。122 发现已知 v1 索引缺表也无法恢复，RED；补充固定结构核验后 123 搜索回归 **12/12**（11376.6323 ms），包括未知版本、真实独占锁不触发替换、损坏文件保留和产品数据字节不变。

121 旧包的真实界面重建失败，RED；124 类型检查与打包完成后，125 搜索/历史桌面回归 **12/12**（26213.7231 ms）。真实损坏文件使界面显式报错，查询/项目/归档筛选、旧结果与草稿保留；点击重建后提示损坏索引已保留，错误消失、结果恢复，未启动执行。归属与恢复代码经独立只读审查；未实测的权限拒绝、搬移中途失败及 sidecar 链接专项仍不冒称覆盖。

实现依据为本地 Node 类型/运行测试、Context7 `/nodejs/node` 的 [DatabaseSync 只读契约](https://github.com/nodejs/node/blob/main/doc/api/sqlite.md)、SQLite 的 [错误类别](https://www.sqlite.org/rescode.html)和 [quick_check](https://www.sqlite.org/pragma.html#pragma_quick_check)。仅 CORRUPT/NOTADB、快速完整性失败或已知版本固定表列损坏进入保留重建；锁定、权限和未知版本不得被当成损坏自动替换。

124 包已更新原目录，app.asar SHA-256 为 `aecee8aac3adeb5b313f6b1a45681cfdb399cbd0fcea20afcad735d1c98b4912`。

127 本地完整回归 **229/229**（320421.4097 ms），0 fail/cancelled/skipped；测试句柄已返回 exit 0。覆盖上述活动搜索与缓存恢复改动，使用 124 实际包，不包含下一节尚未修复的容量问题，也不代替后续提交的 Windows CI。

检查点 `fdcbeb8` 的[文档门禁](https://github.com/mahiro424/AgentX/actions/runs/34283547758)通过，但 [Windows CI](https://github.com/mahiro424/AgentX/actions/runs/34283547764)为 **227/229**，两个新活动搜索测试在全新 checkout 中缺少 `.local-validation/m2-01` 父目录（ENOENT）。133 在独立空工作目录复现 **0/2**；只给这两个夹具补齐自己的父目录，不改产品或放宽断言，134 在另一空目录复跑 **2/2**（2986.8833 ms）。这是测试环境自包含修复，不能把原 CI 失败改写为成功；后续 CI 单独核对。

### 容量实测与剩余缺口

126 使用真实 Electron、实际搜索 IPC 和合成索引：40 个会话、40,000 项、57,909,248 字节，模型调用 0。全命中返回 40 个会话，耗时 225.74 ms，Main 的 16 ms 心跳最大间隔 **234.40 ms**；无命中耗时 82.51 ms，最大间隔 **96.88 ms**。这是明确的同步阻塞证据，不报告为容量通过。下一步需将耗时检索移出 Main 或实现经验证的有界处理，并保持中文字面匹配、排序、覆盖与来源校验；还需检查大量结果的界面响应，不能只优化 Promise 表面。

### 分批检索与完整结果验证

- 128 有命中/无命中的查询不能处理其他事件，有效 RED；改为实际行数受限的批次扫描，批次间释放事件循环，JS 在批次内完成字面计数。129 搜索服务 **13/13**（10336.9809 ms）。不按匹配后的 LIMIT 截断搜索；保留最小 ordinal 的来源、完整相关度计数及中文/标点/ASCII 大小写规则。
- 130 批次间索引更新会混合两版结果，有效 RED；同一连接以 `PRAGMA data_version` 核对每批前后和最终状态，检测到提交后明确要求重查，不持有跨批次事务阻止实时索引写入。131 **14/14**（10923.2494 ms），真实第二连接写入可完成，新查询可重新定位新来源。Context7 资料不足后核验 [SQLite data_version](https://www.sqlite.org/pragma.html#pragma_data_version)与[隐式事务](https://www.sqlite.org/lang_transaction.html#implicit_versus_explicit_transactions)；没有引入 Worker、数据库服务或上游补丁。
- 135 类型检查/打包通过，原目录 app.asar SHA-256 为 `029e4320ec21113d399b0f78e9248015778ade8d6781e31a5342c2245272813c`。
- 136 相同 40 会话/40,000 项的真实包查询：全命中 **427.87 ms**，Main 心跳最大间隔 **63.09 ms**；无命中 **360.62 ms / 32.46 ms**。相较 126，持续响应改善但整次查询延迟增加，不宣称所有性能指标都更快。
- 143 的 500 结果键盘测试在新查询尚未完成、旧结果禁用时抢先聚焦，断言失败；未改产品，修正测试等待实际正文结果可操作。146 复跑 500 会话/40,000 项：完整返回 500 行，查询 **220.51 ms / 31.67 ms**，无命中 **144.24 ms / 31.72 ms**（查询耗时 / Main 最大心跳间隔）；末行上下键选中正确，关闭浮层约 **44.71 ms**，草稿保留。全部模型调用 0，不声称无限数据量的时延保证。补充服务断言确认第 4096 项仍能命中，而非只验证前几项。

### 剩余错误与键盘边界

137 真实窗口验证 Ctrl+K 的 composing 保护、原生模态 Tab/Shift+Tab 限制、组合输入期间 Enter/Escape 不误操作、结束组合输入后 Enter 正常打开。138 的逆序回包测试误用了 Electron evaluate 参数位置，属测试错误；修正后 139 通过，旧查询成功/失败均不能覆盖后到的新查询。140 验证命中定位 IPC 失败保留查询、项目/归档筛选和旧结果，显示错误且可显式重试，成功后仍按真实项聚焦；离开后再次选择须重新读取历史。以上是既有行为的补充回归，不制造产品 RED/GREEN。

144 扩展 journal/wal/shm 硬链接检查通过；损坏保留测试的假 journal 先触发 SQLite READONLY_ROLLBACK，未到预期文件搬移接缝。调整夹具使用独立 shm 后，145 **2/2**（3937.8826 ms）：在文件重命名接缝注入权限失败，分别验证回滚成功/回滚失败均保留原字节、明确报告且不继续读取历史重建。此为文件系统边界故障注入，不冒称实测所有 Windows ACL。

检查点 `a2507af` 的 [Windows CI](https://github.com/mahiro424/AgentX/actions/runs/34284187365)已从实际日志核验 **229/229**（168573.2961 ms），[文档门禁](https://github.com/mahiro424/AgentX/actions/runs/34284187244)通过；不将该 CI 归给尚未提交的分批查询改动。

### 同一真实会话的组织闭环

142 使用 116 已结束的真实 Flash 会话的隔离副本，保留同一产品 task `bae3063e-4419-47a3-9746-ff11b6cd80e7` 与原 thread/turn/item，不复制配置/密钥，设置接口确认没有凭据，新增模型调用 0。未替换搜索、组织、历史或定位的产品 IPC。

经过真实控件完成：改名 → 置顶 → 重开核验唯一行 → 标题搜索打开 → 归档 → 重开确认默认不可搜 → 显式包含归档并重建 → 正文 `PROBE_FINISHED` 定位原命令 → 搜索结果旁显式恢复 → 再次重开。四次真实应用启动均正常退出。

最终仅 title/pinnedAt/archivedAt/organizationRevision 等组织字段按预期变化，组织修订累计 +4；lastActivityAt、执行事实、完整公开历史与命令退出码 0 保持。原合成项目、源产品数据库、源 sessions 和原生历史文件哈希不变，执行接口没有活动任务。实机图：[归档历史定位](images/m2-01/search-archived-real-location.png)、[恢复后的唯一置顶行](images/m2-01/search-restored-real-pinned.png)，均已检查。

独立只读代码审查分为分批搜索、组织存储/Main、Renderer 三个范围；未发现确定的实质缺陷。主代理点验组织事务/CAS、活动归档保护、仅合并组织字段、搜索版本核对和 UI 异步序号。审查不替代当前完整回归、最新提交 CI 与 PR 门禁。

147 当前源码与 135 实际包的类型检查和完整回归 **234/234**（413803.0661 ms），0 fail/cancelled/skipped，原测试句柄正常 exit 0。新增测试包含首末项容量、并发索引更新、sidecar 链接与保留失败、IME/Tab、旧响应和定位失败重试，不借旧 CI 代替当前实现。

## 当前切片边界

| States | 当前覆盖 |
| --- | --- |
| default | 改名菜单、置顶/归档悬停已接通，组织操作不改变执行事实 |
| editing | 当前改名、失败输入保留、CAS 与取消路径已覆盖 |
| keyboard | 改名编辑、多项菜单、禁用项、归档/恢复焦点、搜索打开/关闭、上下键、模态 Tab 与组合输入、精确项焦点已验证 |
| pinned | 置顶、取消、唯一行及重开已接通；197 项完整回归通过 |
| archived、archiveBlocked | 已实现；208 项完整回归含升级/重开及阻断，10 张实机图检查通过 |
| search、matches、indexing、missingSource、emptyError | 有限 IPC、真实浮层、显式/自动索引、后台选中保留、活动 Flash 命中、损坏索引恢复、分批容量、旧响应/错误保留与同一真实历史组织闭环均有对应证据；147 完整回归通过，最新提交 CI 另行核验 |

功能与现场链路、最终完整回归已通过，仍须完成最新提交 Windows CI、PR 与 m2 集成后才关闭 #21、解锁 #22。不提前将功能状态覆盖当成整个 M2 交付完成。

改名版本打包目录为 E:\AgentX\desktop\out\AgentX-win32-x64；app.asar SHA-256 为 72c23b5a07d166974fcda31d74dc052d8c43fb5f50c0ce72863ecd167578de42。本校验只标识本次改名包，后续功能打包会更新，不是安装器或公开发布。
