# M2-03 表格工作台实施记录

日期：2026-09-09。正式 [Issue #23](https://github.com/mahiro424/AgentX/issues/23)，分支 `m2-03`，集成目标仅 `m2`。本文记录正在实施的连续状态，不是工单完成声明。

## 门禁与七项必读

依赖 [PR #32](https://github.com/mahiro424/AgentX/pull/32) 已合入 m2（`d2560f64b43c91cbedd0cf7335b9379f618bf1bb`）；#22 已关闭，#23 为 enhancement + ready-for-agent，原样七项必读的 Agent 简报已经发布。完整父 PRD #1 已打开，范围与状态逐项定位，摘要如下：

1. workbench：沿用唯一输入入口和右侧文件标签，接通表格材料、真实处理、产物核验与续改；不增页面。
2. 状态策略：错误保留草稿/既有内容；读取状态不覆盖引擎终态，不自动重发。
3. US-15/20/21/30：真实表格数值可核对、原件保持、自然语言续改、键盘可达。
4. app-shell：继承当前侧栏/工作区与紧凑按需面板，不重画外框。
5. DESIGN：复用 Token、系统字体、表面/边框、页签/关闭/焦点；不新增品牌规则。
6. 单端 Windows，platforms.md 不适用。
7. 已查看默认 `windows-desktop-workbench.png`，补充 `06-running-spreadsheet.png`、失败态与深色紧凑采用图；旧图 Ctrl+Enter 文案不覆盖当前 PRD 的 Enter 行为。

功能接缝继续为材料核验、不可变版本、有限预览桥、Codex 执行/停止与历史；数据隔离、权威终态与 TEST-M2-VERTICAL-SLICES 不变。

## 当前可操作检查点：材料表格只读

- CSV 使用 UTF-8 严格解码，保留 BOM 后正文、引号/换行、前导零和公式样式文本，不默认转换数值。
- XLSX 保留工作表、稀疏坐标、数值/布尔/日期/错误/文本；公式文本与保存的缓存单列，不运行计算引擎；合并从属格引用主格，不重复计数。
- ExcelJS 4.4.0 与 unzipper 0.12.3 固定依赖，随 Webpack 打包。仅使用本地 Buffer；S3 可选 SDK 不安装，构建保留其缺失时的明确运行错误，不使用空模块替代。
- 解析发生在独立 Electron run-as-node 子进程。Main 只发送核验字节，不传凭据、任意命令或任意 Renderer 路径。最多 2 个并发解析，输入 8 MiB、解压累计 32 MiB、2000 压缩条目、16 工作表、每表 2000 行/100 列、总计 100000 单元格、输出 8 MiB、10 秒超时、192 MiB V8 堆上限；不是 OS 沙箱或全进程内存承诺。
- 主线程退出时请求结束所属解析子进程；真实合成挂起进程通过超时后 PID 已不存在的检查。强杀/断电后的恢复归 M2-05，不能由这一测试冒充已完成。
- 材料登记和预览复用实际路径、打开文件身份、SHA-256、草稿 CAS 与原版本授权；解析后再次核对文件身份。重查不重复启动已知版本解析，也不把旧解析失败改成成功。
- 右侧沿用文件页签，底部工作表可用方向键/Home/End 切换，表格双向滚动，单元格原值/公式及缓存说明可查看。外部变化保留旧视图并标陈旧，关闭返回材料焦点。
- 当前表格发送/生成明确阻断并保留草稿，不将只读材料能力冒称 Agent 已经处理。后续在同一工单接入实际工具、产物和 Flash 两轮验收，完成前不得解除完成门禁。

## 状态覆盖

| PRD 状态 | 本检查点 | 剩余证据 |
| --- | --- | --- |
| readingOffice | CSV/XLSX 真字节解析、材料授权与只读视图已验证 | 真实引擎读取工具 |
| generating | 未开放 | 实际新文件生成与同名/原件保护 |
| validating | 材料格式、类型和公式缓存可核验 | 独立产物核验及任务要求符合性 |
| unsupportedOffice | 格式、编码、宏、加密头、超限、管道错误/超时与损坏可见 | 实际锁定文件及执行工具错误 |
| partialOffice | 未验收 | 失败/停止部分文件及工具进程归属 |
| spreadsheet | 材料的工作表切换、滚动、值/公式、旧视图保留已验证 | 任务产物同一展示链路 |
| revision | 草稿与材料引用复用 | 真实同 thread 续改、产物前后版本和历史 |
| compact | 浅/深色、1280×820/960×640、焦点及双向滚动有图 | 最终生成态再次对照与完整回归 |

## RED → GREEN 与诊断记录

原始日志位于本地隔离目录，不上传凭据或运行数据：

- 001/002：CSV 行为先缺服务而失败，真实进程解析后通过。
- 003/004：XLSX 原先明确未开放，保留两张表、类型和公式/缓存后通过。
- 005/006：超过 2000 行原先返回成功；增加上限后明确失败，不截断。
- 007/008：空 ZIP 被当空工作簿；加入实际 ZIP 结构、宏/加密和解压计数检查后通过。
- 009/010：中文跨 stdout 分块损坏已复现；改用流式 UTF-8 解码后原文一致。
- 011/012：表格材料原先 unsupported；接通既有材料/草稿/预览服务后通过。
- 013/016：原真实包没有工作表；新包真实解析、键盘切换、公式查看、外部变化与关闭焦点通过。
- 014：构建脚本初次误用 Forge 导出，不是产品运行失败；核对实际 `api.package` 后成功。
- 017/018：并发读取原先无上限；最多两个解析，其余明确忙碌，完成后可重试。
- 019：四种尺寸/主题的表格和底部工作表均在窗口内，横纵滚动实际变化。
- 020：本机 1000×100 表格探针实际渲染 100000 格，预览约 1.88 秒，采样 IPC 约 1.3–3.7 ms，关闭约 272 ms。只是本机观测，不是低配设备性能承诺；未据猜测增加虚拟表格框架。
- 021/022：合并格原先重复主格数值；修复为主格引用后通过。
- 023/024：表格本机打开原先被文本白名单阻断；扩展已核验格式后，系统请求与变化阻断通过。此项为系统适配边界测试，不证明外部 Office 完成打开。
- 025：ZIP 依赖固定新版本后，解析/预览 10/10 回归通过。
- 027：真实合成挂起子进程 10 秒超时、退出及环境白名单，解析套件 8/8 通过。
- 028/029：生产依赖许可先缺收集器失败，完整许可收集测试通过。
- 030：ZIP 库的可选 S3 SDK 导致 Webpack 构建失败；已定位为只在 `s3_v3` 使用的延迟 require，不引入云存储 SDK，改为显式外部运行依赖，后续打包结果单独记录。
- 031：上述修复后独立 Windows 包构建通过；032/033 的重开测试先误用桥名 `readDraft`，随后过早读取仍未完成添加的空草稿。按真实 `getDraft` 接口和材料已持久化条件修正测试，不改产品等待时间或放宽失败条件。
- 034：完整本地回归 277/277，通过、无失败/跳过/取消；源解析器测试包含下述无效数值修复，打包快照随后再次刷新以核对一致性。
- 035/036：XLSX 无效数值原先被 JSON 改为 null，现明确失败；1/1 定向回归通过。
- 037：最终只读包重新构建完成，`app.asar` SHA-256 为 `225f96075f1ff9e272ce3976fa964e97e519bef10fa68a5d1e7b1c7de76767cd`；包含完整第三方许可原文。038 正在对该快照进行完整无密钥复跑。

两次独立只读代码核验未发现限定范围内的 P1/P2；这不替代尚未实现的真实表格执行/生成及现场验收。大表已做运行态观测，没有依据猜测扩展新框架。

## 依赖与分发

生产依赖 `npm audit --omit=dev` 在官方 registry 返回 0 项；完整开发依赖审计仍有既有告警，不宣称全部依赖零漏洞。ExcelJS 的 uuid 固定到修复版 11.1.1，保留 CommonJS；依据[维护者安全公告](https://github.com/uuidjs/uuid/security/advisories/GHSA-w5hq-g745-h8pq)，不通过 `audit fix --force` 降级整个 ExcelJS。

打包保留生产依赖及传递依赖许可。saxes npm 包缺少许可文件，随仓库保存[对应版本官方许可](https://raw.githubusercontent.com/lddubeau/saxes/v5.0.1/LICENSE)；构建输出 `resources/THIRD_PARTY_NOTICES.txt`，不改写第三方权属。

目前先验证独立 Windows 包。原 `out/AgentX-win32-x64` 检查时被用户应用占用，未终止或覆盖；最终目录更新、完整 CI、真实 Flash 与 PR 合并仍须各自完成。
