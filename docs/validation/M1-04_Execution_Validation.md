# M1-04 执行链路验证记录（进行中）

日期：2026-09-08。工单 [#9](https://github.com/mahiro424/AgentX/issues/9)，开发分支 `m1-04`。本记录不是切片完成、CI 通过或真实任务验收凭证。

## 1. 固定引擎构建准备

固定官方 Codex `0.153.4`，沿用既有锁定大小与 SHA-256，零补丁。Forge 已接入准备脚本，将二进制、LICENSE、NOTICE 放到 ASAR 外的安装资源。没有使用用户 PATH 中的随机版本，也没有修改用户全局配置。

本地实际验证：

- 官方固定下载地址 → ZIP 校验 → 指定条目解压 → EXE 校验：通过，使用新的独立缓存，不复用 G0 ZIP 来代替下载路径验证。
- 真正打包 Electron 中检查资源、摘要和许可；摘要匹配后执行 `--version`：通过，返回 `codex-cli 0.153.4`。
- 损坏缓存、截断下载、同大小错误摘要：3/3 通过，拒绝使用且不偷偷替换版本。
- TypeScript 检查及隔离目录 Forge 打包：通过。

本地原始证据位于 `.local-validation/m1-04/00-official-download.log`、`00-resource-green.log`、`00-resource-integrity.log`、`01-focus-package.log`。原始目录不上传；准备机制见 [固定资源说明](../../runtime/README.md)。构建准备阶段最初的资源缺失不是业务 TDD 的有效 RED。

## 2. 回归中发现的侧栏焦点问题

首次全量回归为 **58/59 通过**。失败项是原有紧凑侧栏键盘场景：等待“编辑名称”菜单超时。原始失败保留在 `00-full-tests.log`；单独重跑和另外八次重复通过，不能据此认定已修复。

对三个假设分别定位：延迟恢复焦点抢占新焦点、晚到 resize 关闭菜单、原生窗口失焦。源码存在无条件 `requestAnimationFrame` 恢复焦点。新增真实 Electron 回归，受控延迟这次回调，先把焦点移到项目操作，再释放回调：原代码稳定得到 `false !== true`，证明该逻辑确实会覆盖新的用户焦点。首次超时未留事件轨迹，因此不把这项复现写成对首次失败原因的完全证明。

- RED：`01-focus-red.log`，焦点被抢走。
- 最小修复：只在焦点仍为原焦点，或原按钮卸载使焦点落到 body 时恢复；已有新焦点不覆盖。不调整 UI 布局、样式或菜单功能。
- GREEN：重新打包后的原紧凑场景与新增受控焦点场景 **2/2 通过**，见 `01-focus-green.log`。
- 独立只读审查未发现本次资源完整性或焦点修复的阻断缺陷；下载失败可能留下的临时文件在安装资源目录之外，保留诊断不视为安装成功。
- 修复后真实打包应用的全量回归 **60/60 通过**（116643.8704ms），见 `01-full-tests.log`。这是本地自动验证，不等于 CI 或真实模型验收。

## 3. Main 内部 stdio 传输接缝

新增 `src/main/runtime/codex/transport.ts`，仅供 Main 的 Codex 适配模块消费，不向 Renderer 暴露 RPC。当前覆盖按 ID 配对请求、交错通知、UTF-8 跨块输入、服务端请求单次回应、断线/超时失效、迟到流错误和协议异常。传输层不把审批写入成功或通知到达解释为工具成功/轮次完成。

实际固定二进制子进程已通过该传输层完成 `initialize → initialized → config/read`。测试使用独立 CODEX_HOME、合成凭据和本机拒绝服务地址，配置读取确认 `deepseek-v4-flash` 与 DeepSeek provider；服务端没有收到模型 HTTP 请求。这里没有创建 thread 或 turn，也不读取正式凭据。

验证记录：

- `02-transport-before.log` 是模块尚未落地的装配失败，不冒充产品业务 RED；随后真实边界测试通过。
- `03-approval-red/green.log`：服务端审批只登记，显式决定回写一次，重复决定失败。
- `05-handshake-red.log`：真实 initialize 已应答，但缺少 initialized 通知接口；补齐后握手与配置读取通过。
- `06-close-red/green.log`：关闭后迟到 EPIPE 不得作为未处理异常崩溃 Main，修复后通过。
- 独立只读审查发现混合请求/应答字段和断线处理回调自身抛错的问题；`07-mixed-*`、`08-handler-*` 分别记录 RED/GREEN。错误日志不包含原始协议正文或异常中的私有内容。
- 审查提出的“单请求超时使整个连接失效”保留为本阶段有意的保守合同：应答未知时不继续执行或重发，交给产品协调层核对。`09-transport-tests.log` 明确验证这一行为。双向请求 ID 空间独立，不因客户端与服务端恰好同号判冲突。
- 全量回归 `06-full-tests.log` **67/67 通过**（101858.4198ms）；其后只新增超时合同测试，最新传输/真实握手定向 **8/8 通过**。TypeScript 检查通过。未把定向数量拼成一次不存在的 68 项全量运行。

这些测试证明传输接缝及实际固定进程握手，不代表产品任务可提交。进程启动/配置供给目前由隔离测试驱动；生产进程拥有者、生成类型适配、任务意图持久化及 Renderer 接线还未接通。背压与输出体积边界应在接入真实工具输出时验证，不声称已有无界大输出处理能力。

## 4. 固定版本握手适配

已逐字节采用 `0.153.4` stable 生成的六文件握手类型依赖闭包及摘要清单，未改写生成文件或既有 G0 元数据。`initializeCodex` 使用生成类型构造非实验握手，并运行时校验必要字段、Windows 平台与已知规范化数据目录；校验通过才发送 `initialized`。错误目录、错误平台或缺失字段均关闭逻辑连接，不能继续发起轮次。

真实固定子进程测试已改为调用产品的 `initializeCodex`，不再在测试里独立拼装握手流程。`10-typed-green.log` 验证实际握手/配置读取；`11-handshake-contracts.log` 记录通信及握手专项 **9/9 通过**，含错误返回不发送 `initialized` 的检查。TypeScript 检查通过。没有创建模型 thread/turn，没有真实模型请求。

生成类型和握手校验不是完整执行适配器：进程创建及配置供给仍由隔离测试驱动，产品进程拥有者和任务提交尚未接通。此阶段未再次执行全量桌面回归，上一节全量计数不改写。

## 5. 固定进程拥有者

`src/main/runtime/codex/process.ts` 已接管固定安装资源定位、大小/SHA-256 校验、无 shell 隐藏窗口启动、明确环境供给、握手和该 App Server 进程回收。只接受 Main 内部传入的目录/环境，不隐式继承当前全部环境；移除其他 `CODEX_*` 环境覆盖并指定本实例 CODEX_HOME。启动参数固定 Flash、DeepSeek provider、请求批准及 workspace-write，不从 PATH 另找版本。

真实子进程测试现在调用 `openCodex`，不是测试自己实现 spawn/握手/kill；配置读取确认模型、provider、审批和沙箱，关闭后确认对应 PID 不再存在。损坏安装资源测试证明在创建子进程前拒绝，并保留原文件。`12-process-green.log`、`13-process-guards.log` 留证；最新进程/握手/通信专项 **10/10 通过**，TypeScript 检查通过。未调用模型，未启动工具子进程。

这里的关闭只验证该 App Server 的回收，不代表 M1-06 的活动工具进程树清理或运行中停止已通过。退出会有等待期限，未确认退出明确报错，不冒充已回收。stderr 不复制到普通日志或 Renderer，启动错误保留阶段原因、进程退出码和 stderr 字节量；更细的脱敏诊断仍需随产品错误显示接线完善。

产品配置生成/凭据冻结、执行协调服务以及 Main/Renderer 入口尚未调用此拥有者；不因内部模块已可运行而开放发送按钮。本节之后未重跑完整桌面测试，上述全量计数保持原始记录。

## 6. 模型设置到执行凭据快照

`ModelService.captureExecution` 是 Main 专用交接：校验配置修订、保存结果、连接启用、Key 引用、已选 Flash、目录存在 Flash 以及已知失败测试；系统解密后再次校验修订与凭据引用，返回冻结的本轮模型/修订/凭据快照。包含明文的快照不属于 `ModelSettings`，没有加入 Preload 或 IPC。

`tests/desktop/execution-model.test.cjs` 在真实 Electron Main 中加载当前服务源码，使用临时产品目录及实际 safeStorage 加解密。它验证正确的 Flash 快照、普通设置读取不含密钥，以及受控延迟系统解密期间关闭连接会拒绝旧快照。模型目录 HTTP 是边界替身，真实模型调用为零。此测试属于 Main 服务接缝，不宣称是 Renderer 提交闭环。

`14-model-capture-red/green.log`、`15-model-race.log` 留存过程；新方法完成后重新打包，`15-full-tests.log` 为 **72/72 通过**（142774.6521ms），TypeScript 和构建通过。

独立审查确认 Main 普通读取没有返回明文，并提出其余阻断路径覆盖建议。没有新增“必须先有成功测试才可执行”的产品门禁：PRD 允许未测候选说明，当前合同只阻断已知失败/不兼容，遵循既有 ModelService 选择语义。快照只核验固定 Flash 的实际执行条件，不为配置文件加第二套完整性系统。协调器后续必须以该快照作为本轮有效配置，禁止把设置的新修订混入本轮或经 UI 返回明文。

尚未接入执行协调与发送入口；只拥有这个快照不能声称任务已经启动。

## 7. Flash 配置供给

`configuration.ts` 已把固定 Flash 模型目录、最小系统环境和公开配置参数交给进程拥有者。采用 G1 已核验的官方单一 Flash 目录，原始字节与 SHA-256 一致，没有复制 G1 的自动拒绝审批或样例任务编排。其他模型在任何目录写入前拒绝。

产品自管字段用公开启动 `-c` 覆盖供给，不整份重写用户可能已有的 `config.toml`；这只是配置供给位置变化，不更改引擎或权限设计。`models.json` 已存在且与固定目录不同时明确失败并保留原件。密钥只加入专属进程环境，不进入命令行或目录文件；shell 白名单在加入密钥之前生成。

真实固定引擎已从该生产配置读取 Flash、DeepSeek 地址、env_key、非提权 Windows 沙箱、shell inherit/白名单，并通过公开 `model/list` 读取唯一 Flash 条目。测试使用合成 Key 和本机网络拒绝代理；无 provider 请求，没有创建 thread/turn。

`16-config-*`、`17-prepared-*`、`18-config-runtime-tests.log` 留证；最新配置/进程/握手/通信 **13/13 通过**，TypeScript 检查通过。首次启动失败是未接入配置参数导致的装配失败，不记作业务状态 RED。新增配置文件保护与模型门禁测试已纳入 npm test；本节之后未重跑全量桌面测试。

仍需由执行协调器调用快照、配置和进程拥有者，核对实际加载的项目配置/MCP 来源，完成发送意图落盘后再开放提交。不能把配置里没有 shell Key 当作活动工具最终边界已验收。

## 8. 发送意图与失联落盘

schema 6 新增 `execution_intents`。首次任务和发送意图在同一事务写入；只保存输入、模型、配置修订和凭据引用，不保存模型明文密钥。重复操作 ID 导致事务失败时，第二个任务也回滚；v5 升级前留存可读取的 `agentx.before-v6.<uuid>.db`，不清空旧项目。

`20-full-tests.log` 记录本次迁移及前述配置供给完成后的完整测试 **78/78 通过**（146612.9681ms），隔离目录真实打包及 TypeScript 检查通过。此前各节的局部计数保持历史事实。

随后增加 Main 内部发送状态接缝：RPC 写入前只允许 `prepared → sent` 一次；发送后不确定时，意图 `unknown` 与任务 `reconciling` 原子提交。任务 ID 不匹配、重复发送、过期观测均拒绝；任务更新失败时意图更新回滚。原输入保留，不自动重发。`21-uncertain-red.log` 留存缺少实现的失败，`21-uncertain-green.log` 为四项存储专项通过，TypeScript 检查通过。这是存储行为，不代表真实断线、RPC 发出或 UI 核对链路已验证；新增转换之后尚未重跑全量打包测试。

## 9. 首次 thread/turn 协议接缝

`execution.ts` 使用固定 0.153.4 生成的请求类型构造 `thread/start` 和 `turn/start`：显式 Flash、DeepSeek、用户审批及持久化 thread。thread 返回的模型、provider、项目目录、审批路由和沙箱不一致则拒绝发送输入；只接受项目本身的额外可写根和关闭网络的 workspace-write。turn 应答只返回已确认 ID，不将它的 status 当作完成事实。

新增请求类型及依赖逐字节采用固定生成批次，当前 manifest 共 25 文件，源文件字节与 SHA-256 核验 25/25。`22-execution-red/green.log` 和 `23-execution-guards.log` 留存测试；协议与存储专项合计 6/6，TypeScript 检查通过。协议测试使用真实 transport 与内存流边界响应，无真实 thread、turn 或模型请求。

独立审查未发现固定协议字段/枚举映射错误。边界：当前只校验返回中实际消费的必要字段，不宣称完整 response schema 验证；目录实时可用性仍由协调器负责；指令来源仅保留列表，不能冒称共享技能已隔离。尚未通过产品界面调用这两个方法。

新增协议接缝和失联存储转换后，重新生成独立目录的真实 Electron 包并执行完整自动测试：`24-package.log` 打包成功，`24-full-tests.log` **81/81 通过**（125467.879ms）。该计数包含桌面、固定引擎配置/资源、协议及存储测试，不是 81 次真实模型任务；模型调用为零。

## 10. 首次提交的协调与关联

`services/execution.ts` 的 Main 内部 `submitFirstTurn` 串起真实存储与协议接缝：无活动/未决任务时，同步检查并保存任务和意图，标记发送后调用 thread/start；thread 关联落盘成功才调用 turn/start；轮次应答与原操作匹配后原子保存 turn ID 和 acknowledged。已有活动或未决任务时拒绝，不自动排队。调用者仍须供给冻结配置和已握手的专属连接；这里不接受 Renderer 原始 RPC。

失败路径保留已经确定的 thread ID、原输入与未决意图。turn 应答丢失进入 reconciling，不能自动补发；核对状态写入也失败时显式报告双重失败，保留原记录。线程不能被重新绑定，错操作的轮次确认事务回滚，不留下半条关联。

`25-submission-red/green.log` 记录首次协调行为的 RED/GREEN；`26-submission-disconnect.log` 覆盖应答丢失；`27-submission-bindings.log` 为协调/存储/协议专项 **9/9 通过**，TypeScript 和 diff 检查通过。测试经过真实 SQLite 与 stdio transport，响应端为内存流边界替身；没有启动真实模型会话。新测试已纳入 npm test，本节改动后尚未重跑全量打包测试。

这是首次发送顺序的生产代码，不是已交付的工作台闭环：冻结配置/进程拥有者的调用入口、早到事件缓冲与归属、审批/停止、任务切换和 Renderer IPC 尚未接通。不能将返回的 running 观测当作重开后仍在执行；持久化读取仍需核对。

## 11. 匹配轮次的终态落盘

Main 的 `observeTurnCompletion` 仅处理 `turn/completed` 通知，识别 completed/failed/interrupted；普通请求应答不走终态入口。`settleTaskTurn` 在同一事务中核对 task/operation/thread/turn、当前非终态及意图状态，再更新任务终态、活动时间和 settled。错 thread、错 turn、错 operation、已处理的重复通知均不能改写当前记录。通知将 inProgress 冒充终态时明确报错，不伪装完成。

`28-terminal-red/green.log` 记录新增终态处理的失败与通过；`29-terminal-guards.log` 为协调/存储/协议专项 **9/9 通过**，类型检查通过。测试验证从首次提交到匹配完成通知的 Main 内部数据链路，协议响应仍为边界替身，不是实际模型完成了工作。

当前函数须由当前连接的事件拥有者调用；早于 turn/start 应答到达的通知尚未缓冲，连接世代失效、真实进程回调接线及失败结果正文展示尚未完成。因此终态存储通过不代表整个执行事件链路已交付。本节后未重跑全量打包测试。

## 12. 早到终态与单连接失效

`FirstTurnSession` 一个实例只属于一次首次提交及一个连接。早到终态仅缓冲必要关联字段，等 thread/turn 应答落盘后按绑定处理；同实例不能重复发送。连接失效后停止接受旧回调，已确认的活动轮次与意图原子转为 reconciling/unknown；已收到并落盘的终态不回退。未决请求失败仍由首次提交协调处理，不自动重发。

`30-early-red/green.log`、`31-session-disconnect-red/green.log` 留存两次行为循环；专项 **11/11 通过**。独立只读审查未发现已覆盖路径的状态错写，提醒真实调用入口必须使用 Session 的回调，不得绕过失效检查直调底层终态函数。早到事件落盘失败、实际连接回调与所有工具事件的整合仍需后续验证，不以当前成功路径代替。

重新生成独立目录 Electron 包并全量回归：`32-package.log` 打包通过；`32-full-tests.log` **86/86 通过**（131154.692ms），TypeScript 通过。期间未读正式凭据、未调用真实模型，也没有关闭用户原有应用窗口。

## 13. 早到终态落盘失败回归

补测确认了一个真实遗漏：早到终态在 submit 内缓冲处理时，如果 SQLite 拒绝保存终态，原 catch 只使回调失效，数据库仍为 running/acknowledged。`33-save-failure-red.log` 明确记录实际 running 与预期 reconciling 不符；故障通过临时数据库触发器注入，没有替换内部存储实现。

现在已绑定轮次的缓冲处理失败会先使会话失效，再尝试保存 reconciling/unknown，保留原 thread/turn。若核对状态也写不进去，抛出包含两项错误的聚合异常，不冒充保存成功。`33-save-failure-green.log` 协调/存储/协议专项 **12/12 通过**，TypeScript 和 diff 检查通过；尚未新增双重写入失败用例。当前修复之后未重跑全量打包，86 项记录属于上节基线。

实际进程入口接线前已检查 Main 现状：尚未创建执行服务，也未开放执行 IPC；原有“本阶段没有运行任务”的界面崩溃提示须随入口接线更新。固定协议 `config/read` 支持 cwd 与 includeLayers，后续使用其核对实际项目配置，不能只靠构造参数声称生效。

## 14. 实际生效配置与执行进程入口

`verifyExecutionConfiguration` 使用固定 stable `ConfigReadParams` 对项目 cwd 读取 includeLayers 配置，核对实际 Flash/DeepSeek、Responses 地址与 env_key、请求批准、workspace-write、关闭沙箱网络、Windows 非提权模式和 shell 最小环境键。错误不拼入原始配置或敏感值。当前核对产品控制的关键字段，不宣称完整配置 schema 校验、MCP 来源治理或技能隔离已完成。

`openExecutionCodex` 组合固定资源进程启动、握手和上述核对；只有核对成功才交出连接。失败时回收本实例进程；回收也失败则明确报告两项错误。底层 openCodex 仍供协议/资源探针，产品入口应使用已核对版本。

`34-effective-*`、`35-effective-guards.log`、`36-verified-process-*` 和 `37-verified-cleanup.log` 留证。真实固定引擎正常配置读取成功；注入错误 provider 地址时被拒绝，并确认该子进程 PID 已不存在。协调/存储/协议/配置及真实进程专项 **19/19 通过**，没有 thread/turn 真实调用，合成 Key、本机网络拒绝代理的请求记录为空。ConfigReadParams 原始字节加入 manifest，当前共 26 文件。此后尚未重跑完整打包测试。

## 15. 停止请求与权威终态分离

`FirstTurnSession.stop` 只使用本实例已确认的连接及 thread/turn。先以原 task/operation/轮次关联将活动任务落盘为 stopping，再经固定协议 `interruptTurn` 发出 turn/interrupt；空应答不改变 stopping。重复点击、无有效轮次、终态任务或失效连接不再次发送。中断应答丢失则保持原关联并进入 reconciling/unknown，旧回调不能解锁。

`38-stop-red/green.log` 覆盖同一连接的实际 transport 写入顺序；匹配 interrupted 通知到达后才结束。`39-stop-disconnect.log` 协调/存储/协议专项 **13/13 通过**，类型检查通过。使用内存流模拟引擎响应，不代表真实活动工具已停止；该副作用验收仍需 Flash 人工现场测试。固定 TurnInterruptParams 原始字节已采用，manifest 共 27 文件。

尚未把停止方法接到真实桌面按钮，也未完成原生审批与补充控制；不提前开放看似可用的工作台入口。本节之后尚未重跑全量打包测试。

## 16. 当前轮次补充要求

`steerTurn` 采用固定 TurnSteerParams，必须带 expectedTurnId；请求只有文本输入，不覆盖模型、目录或权限。正常 RPC 拒绝原样按脱敏错误报告，不改成 turn/start；应答关联不符则失效连接，不能冒充接收成功。manifest 当前 28 文件。

`FirstTurnSession.steer` 只接受本连接绑定且仍活动的轮次，阻止并发补充；stopping/未知/终态不补发。补充尚在等待应答时，停止仍可发出，补充应答也不能把 stopping 改回 running。函数不清除调用者输入，后续 Renderer 按已确认方案保留失败草稿。

`40-steer-red/green.log`、`41-session-steer-red/green.log`、`42-steer-stop.log` 留证，专项 **14/14 通过**，类型检查通过。覆盖 expectedTurnId、拒绝不新开轮、并发补充门禁以及补充期间停止。没有真实模型请求，真实补充效果与 UI 草稿仍待产品链路验收。

后续重新打包隔离目录的真实 Electron 并执行全量自动测试：`43-package.log` 打包成功，`43-full-tests.log` **90/90 通过**（132874.7854ms）。此结果包含上节停止、实际配置核对及保存失败修复；不表示真实模型验收完成。

## 17. 原生审批请求失效

transport 的待响应请求现在记录 requestId 与 threadId 关联。匹配的 `serverRequest/resolved` 在分发给上层之前移除待回应资格；其他 thread 的 resolved 不误删当前请求。已解决请求的迟到允许/拒绝不再写入 stdio。通知仍交给上层，用于后续禁用对应卡片，不把 resolved 等同于批准或工具成功。

`44-approval-resolved-red/green.log` 验证原实现会错误回应已解决请求，修正后通信专项 **8/8 通过**，TypeScript 检查通过。原生两类审批的外层 JSON-RPC id 才是响应标识，command params.approvalId 不替代它；仅本次允许/拒绝将使用 accept/decline，不增加会话级自动授权。此后尚未重跑全量打包，审批列表、决策入口与界面仍未交付。

## 18. 原生审批解析与仅本次决策

`runtime/codex/approvals.ts` 识别 commandExecution/fileChange 的原生请求，保留外层 requestId、thread/turn/item、开始时间及必要说明；命令区分 command/writeStdin，并保留工作目录、环境和网络目标。文件 grantRoot 仅作请求说明保留，不依赖不稳定字段实现会话授权。未知请求类型明确不支持，不自动批准。

响应使用固定版本生成类型，只接受 accept/decline；acceptForSession 或策略扩展在发送前拒绝。回应原始外层 requestId，不使用 params.approvalId；重复回应由 transport 拒绝。`46-approval-red/green.log` 审批及通信专项 **9/9 通过**，类型检查通过。测试已加入 npm test；固定类型及依赖 manifest 当前 35 文件。

此处是 Main 内协议适配，尚无产品审批 token、当前轮次归属检查和审批 UI；不能直接向 Renderer 暴露该原始请求对象，也不能把适配测试等同真实文件/命令已获批准。后续由 Session 管理当前连接的请求集合及失效，再经有限产品 DTO 展示。

## 19. 会话审批集合与当前轮次失效

Session 现在以产品审批 token 管理请求集合，原始 RPC requestId 留在 Main 内。早到请求在 thread/turn 绑定后归属核对；多个审批分别回应，回答一个不会清掉其他待决请求。写入应答不等于解决，匹配 resolved 到达后才解除对应等待；停止、终态、断线后旧请求不可回答，终态后的迟到请求展示为失效。

`47-session-approval-*`、`48-late-approval-*`、`49-stop-approval-*` 保留 RED/GREEN；最后一组通信、审批、协调及存储专项 **21/21 通过**。`50-package.log` 隔离 Electron 打包成功；`50-full-tests.log` **93/93 通过**（110567.3138ms），类型检查通过。没有真实模型调用。

独立审查提出 resolved 先于对应 request 的逆序假设；目前尚无固定引擎实际会这样发送的证据，不把假设写成已复现缺陷，也不新增推测性的逆序恢复机制。测试覆盖的是同一 stdio 连接按顺序分发及应答之前已解决的请求。审批卡片仍待产品 IPC/UI 接线。

## 20. Main 执行入口的准备门禁

`ExecutionService.start` 开始组合已有模型快照、项目目录核对、固定配置、已核对进程与 FirstTurnSession。输入只接受产品项目/任务/操作标识、配置修订、Flash 和纯文本，不接受 Renderer 提供 cwd 或原始 RPC。准备阶段在异步解密之前占用串行槽；重复任务/操作或已有活动/未决任务拒绝，不排队。目录从项目记录读取并重新核对，凭据不进入返回值或发送意图。

`51-service-red.log` 记录缺少入口的失败，`51-service-green.log` 验证模型阻断时不创建任务、不准备引擎目录，并发请求不会再次捕获凭据。`52-service-regression.log` 协调、存储与入口专项 **14/14 通过**；新增固定资源缺失回归使用合成 Key，实际配置准备后在固定 EXE 检查处失败，没有启动引擎或访问模型，修复前重复尝试仍明确失败且没有假任务。测试已加入 npm test，TypeScript 检查通过。

当前入口尚未由应用 Main 实例化，成功提交路径尚未通过该入口做真实桌面验证；不要把这两个门禁测试表述为真实任务闭环。公开执行快照/事件、有限 IPC、界面与入口成功路径测试仍需继续完成。93 项全量记录是本节改动之前的基线，不能替代本节后的完整回归。

## 21. 正文、命令与文件执行项

固定协议事件适配现在提取 agentMessage 生命周期/正文 delta、commandExecution 生命周期/输出 delta，以及 fileChange 生命周期。Session 按 thread/turn/item 保存本连接的执行项；早到正文在绑定后可读取，其他轮次不进入当前快照，断线及轮次终态后不再采纳旧片段。正文完成快照替换累计正文；命令保留实际输出、退出码、耗时和失败/拒绝状态，不把 item/completed 的通知名称直接翻译为操作成功。

文件变化保存引擎报告的 path、add/delete/update、move_path 与 diff。读快照深复制，调用者不能改写内部消息或嵌套文件变化；这些报告还不是 M1-05 对磁盘实际变化的独立核验。未处理的 patchUpdated、文件输出 delta、计划事件等需继续按真实通知补齐，不能把当前三类适配称为全部事件支持。

`53-items-*`、`54-command-*`、`55-file-*` 分别留存 RED/GREEN，最后一组 Session 专项 **8/8 通过**。`56-item-state-*` 验证并修复数组状态经字符串转换被误接受的问题，现要求真实字符串枚举。TypeScript 检查通过，测试加入 npm test。独立只读审查核对固定生成字段、跨轮过滤、深复制和终态保护，未发现这些已覆盖路径的确定错误；绑定前未归属事件的内存暂存、尚未支持的事件是明确边界。

`57-package.log` 新隔离包打包成功；`57-full-tests.log` 全量 **97/97 通过**（152752.1547ms），没有失败或跳过。未读取正式 Key，也未调用真实模型。事件尚未通过产品 IPC 送到实际 Renderer，用户可见工作台仍待接通。

## 22. 产品执行快照、发送与停止 IPC

Main 已实例化 ExecutionService，Preload 增加 getExecution/startExecution/stopExecution/onExecutionChanged 四个有限接口。每个 IPC 仍核对产品主窗口、主 frame、URL 和参数个数；服务验证产品输入及当前 task/thread/turn，控制操作标识不能重复使用。快照不含原始 RPC requestId 或凭据。运行状态通知只提示重新读取快照，不把任意引擎消息直接透传给 Renderer。

准备阶段、消息/审批更新和断线触发状态通知；服务错误保留在快照和调用异常中。退出先阻止新发送并等待准备完成，回收本实例引擎；回收失败明确阻止静默退出。此处不是 M1-06 的托盘保活、活动任务退出确认或进程树副作用验收，不提前声称后者完成。

`58-snapshot-*` 验证准备中的快照、错误保留和无有效轮次停止拒绝。`59-bridge-red.log` 在真实旧 Electron 窗口确认缺少产品接口；新增实现后 `59-package.log` 隔离打包成功，`59-bridge-green.log` 真实产品桥专项通过。补充的测试经实际 Main 模型配置拒绝缺 Key 的发送，收到准备/结束通知，任务库仍为空；没有模拟 Renderer 成功或启动真实模型调用。

独立审查提出重复 before-quit 会绕过回收的疑点；主代理核对实际 index.ts，event.preventDefault() 在 exiting 判断之前，每次未确认回收都阻止退出，因此该具体指控不成立。活动任务关闭的副作用仍需后续验收，不能以此次代码核对代替。

`60-full-tests.log` 真实全量为 **97/98 通过**，唯一失败是原 IPC 精确白名单尚未包含本节批准的四个新方法。已只补入这四项，保留精确键集合、Object.freeze 和无 Node 权限断言，不放宽为任意接口；修正后 `61-full-tests.log` 完整回归 **98/98 通过**（127152.0099ms）。工作台组件尚未消费执行桥，补充与审批产品 IPC、实时项目状态投影仍待接通。

## 23. 补充与审批产品控制

ExecutionService 新增 steer/answer，并接到独立 Preload 方法与 Main IPC。请求必须关联当前 task/thread/turn，携带一次性产品 operationId；补充文本和审批 token/decision 先校验，再由原 Session 回答原连接。仅 accept/decline，不能携带会话级授权，也不能将补充改发成新的 turn/start。

`62-control-red/green.log` 通过真实产品配置准备、SQLite、ExecutionService、Session 和 CodexTransport 串起首次提交、补充、审批、停止。此测试只在进程创建边界使用内存 stdio 对端及合成凭据，不调用模型；断言原生审批外层 id、重复控制拒绝、只有一个 turn/start、停止应答保持 stopping、匹配 interrupted 后才结束，以及产品快照不含合成 Key。控制/会话专项 **6/6 通过**，TypeScript 通过。

`63-control-bridge-red.log` 确认旧真实窗口缺少补充方法；`63-package.log` 新隔离包成功，`63-control-bridge-green.log` 桌面产品桥专项通过，验证无有效轮次时补充/审批明确拒绝。精确 Preload 白名单只追加两个批准接口。此后未重跑全部测试，98 项全量通过记录属于上节基线；实际 Flash 控制副作用、工作台组件及现场验收尚未覆盖。

## 24. 工作台运行态与单一停止主按钮

重新打开已采用 workbench 默认图及 running 变体，并按既有 Token 实现运行态：执行过程在上、同一个输入组件在下，主按钮从发送箭头切为停止方块。正文保留换行；命令项可展开实际输出、退出码和耗时，失败显式标红；文件项展示引擎报告并说明尚待实际文件核对。不额外创建任务管理页面，也不把尚未接通的首发按钮开放。

useExecution 通过实际 Preload 订阅通知并重新读取快照，序号隔离迟到读取；界面只显示当前选中任务的执行项。停止经过产品 IPC，草稿不清空，空应答不将 stopping 画成 interrupted。`65-workbench-red/green.log` 记录真实 Electron 工作台从不显示过程到运行态通过；`66-running-qa.log` 覆盖浅/深色与 960×640 的主按钮可达及无横向溢出，截图为合成 IPC 边界测试数据，不是真实 Flash 会话或产品验收结果。已有侧栏活动投影尚未整合，不能将本次截图宣称为全页面设计 QA 完成。

`67-package.log` 新隔离包成功，`67-full-tests.log` **100/100 通过**（151003.4779ms）。独立审查随后发现两项具体 UI 边界，继续逐项验证：停止应答后、状态读取之前按钮短暂重新可用，以及旧任务停止错误出现在新会话。

`68-stop-latency-red.log` 首次采样未复现；补上实际渲染帧后 `68-stop-latency-red-confirmed.log` 确认禁用预期失败。现等待最新状态读取落地，或收到匹配非运行状态，才解除本任务的本地停止等待；`68-package.log` 打包和 `68-stop-latency-green.log` 桌面回归通过。错误归属用例 `69-error-owner-red.log` 确认新会话错误残留，现错误携带 taskId，仅在对应任务显示；`69-package.log` 新隔离包成功，`69-error-owner-green.log` 桌面回归通过，类型检查通过。100 项全量记录是这两项修正之前的基线。

首发/补充/审批交互、草稿分任务保存、活跃侧栏投影与真实 Flash 接入仍待完成。本轮没有模型调用、上游补丁、master 合并或公开发布。

## 25. 补充要求与就地审批交互

运行中的补充要求使用独立文字动作和 Ctrl+Enter，主按钮仍为停止；补充应答失败保留草稿，成功后仅清除没有继续编辑的原输入。输入修订也在切换任务时推进，迟到应答不清除新编辑内容。审批卡位于对应执行项附近，展示协议提供的命令、目录、原因及范围；提供允许本次/拒绝，回答中、已解决及失效请求不能重复回答。未提供文件细节时明确说明，不编造文件列表。

`70-steer-ui-red.log` 与 `71-approval-ui-red.log` 分别记录旧窗口缺少补充动作及审批按钮；对应 package / green 日志证明新真实 Electron 窗口专项通过。桌面用例在有限 IPC 边界注入合成执行数据，没有调用模型；覆盖补充失败保留、发送期间继续编辑、停止仍可用、两个审批分别允许/拒绝以及已解决按钮禁用。

`72-full-tests.log` 全量 **100/100 通过**（520438.3498ms），包含前述补充和审批 UI。它不是 Flash 实际副作用或整个 M1 验收证据。首发 UI、草稿持久化、侧栏实时状态、其他必要事件及真实任务验收仍需继续完成。

## 26. 工作台首次发送

输入主按钮已接到真实 startExecution 产品接口，不再永久禁用。发送前显示项目、模型或串行占用的具体阻断原因；请求携带新的 taskId/operationId、选定项目、Flash 和模型配置修订。同步 ref 锁加提交状态阻止重复点击；失败保留输入并核对产品任务记录，已创建的未决任务不自动重发。确认返回后进入对应任务，期间继续编辑的内容不被清除。另一活动或待核对任务可定位，不自动排队。

`73-submit-red.log` 确认旧包不能从工作台发送。新增实现经 `73-package.log` 打包，`73-submit-green.log` 专项通过。此项在真实 Electron 窗口操作，在 startExecution IPC 边界模拟延迟及失败，验证 UI 的请求归属、防重及草稿行为；不是模型执行成功证据，生产路径没有对应模拟逻辑。

独立审查发现项目切出又切回后，旧应答仍会导航用户。`74-submit-navigation-red.log` 已复现；现每次项目/任务选择变化推进导航代次，只有原输入现场仍未离开才自动进入结果。`74-package.log` 新包成功，`74-submit-navigation-green.log` 首发、导航边界与既有运行控制 **3/3 通过**（28074.4346ms），TypeScript 检查通过。

既有 foundation 禁用用例仍要求“执行未接入”旧文案，`75-foundation-note-before.log` 如实记录失败；将预期改为当前实际原因“请先选择本地项目”，保留按钮禁用、草稿保留及未开放入口检查。完整新版回归尚待结果，不以此前 100 项结果替代。

`75-full-tests.log` 随后完成，新首发 UI 基线全量 **102/102 通过**（147748.6353ms）。本结果在下节侧栏投影修改之前产生。

## 27. 侧栏实时执行投影与固定交付目录

ProjectService 在目录探测结束后读取本实例 ExecutionService 的当前任务。只替换 taskId、projectId 和目录均匹配的记录；其他落盘非终态仍按旧观测显示待核对。实时状态来自既有执行协调与存储，不在 Renderer 或项目服务另造任务状态机。

`76-projection-red.log` 确认旧服务将当前 running 错画为 reconciling；修改后 `76-projection-green.log` 通过。`76-projection-integration.log` **4/4 通过**（1816.0044ms），覆盖同一真实服务/存储/内存 stdio 协议测试链路的 waitingApproval → running → stopping → interrupted 投影，以及无拥有者/归属不匹配时保持待核对。没有真实模型调用。

独立审查将需求中的运行、待批准、停止中示例误解为仅允许三态白名单，建议屏蔽 submitting/interrupted 等。该意见未采用：PRD UI-M1-WORKBENCH 明确要求提交、完成、中断及状态核对均可观察，匹配的本实例权威状态不能人为退化为 reconciling。

用户要求每完成一个功能更新固定 `out/AgentX-win32-x64`。本次检查没有该目录下运行中的应用后，`76-out-package.log` Forge Windows x64 打包成功，目标为 `E:\AgentX\desktop\out\AgentX-win32-x64\AgentX.exe`。包包含当前已实现功能，不代表 M1 完整验收；自动桌面检查使用独立数据目录。

`76-out-desktop.log` 对这份实际 out 包执行默认空工作台、项目 IPC 边界和两项首发交互专项，**4/4 通过**（10112.4459ms）。未将整个 102 项旧基线结果冒称为新投影版本的全量结果；最新投影已通过上述定向集成与桌面验证。

## 28. 设置变更的下轮生效提示

设置页现在消费全局执行观测，而不是只看当前选中的会话。准备、提交、运行、待批准及停止中显示“新配置仅对下一轮生效”，明确关闭连接不会停止当前轮，并提示回任务执行停止。未能读取执行状态或处于核对状态时，不声称已经停止。关闭连接仍由真实模型配置服务持久化，不向引擎中途替换凭据。

`77-pending-effect-red.log` 记录旧包没有提示；TypeScript 检查通过，`77-isolated-package.log` 隔离包成功，`77-pending-effect-green.log` 工作台桌面流程通过（48120.6522ms），覆盖进入设置、提示可见、通过实际模型配置 IPC 关闭连接后原执行观测仍为 running，以及返回后的补充、审批和停止回归。执行数据为 IPC 边界合成数据，无真实模型调用；不能替代真实 Flash 运行中更换配置验收。

首次更新 out 时发现用户窗口占用，未覆盖或结束用户窗口；随后确认窗口已关闭，`77-out-package.log` 记录指定目录打包成功。`77-out-smoke.log` 对这份实际包的默认空工作台启动检查 **1/1 通过**（12458.7405ms），测试使用独立数据目录。

## 29. 草稿存储与有限产品接口

产品 SQLite 升级至 schema 7，新增独立 drafts 表，按未关联、项目与具体任务隔离。读取不启动引擎；保存核对任务属于指定项目，文本和修订受限。保存事务比较 expectedRevision，旧应答或旧版本不能覆盖新草稿；失败返回明确错误。迁移前保留 `agentx.before-v7.<uuid>.db` 一致性快照，不更改旧备份。空工作台首次读取不存在的草稿不会因此建立数据库。

`78-drafts-red/green.log` 覆盖存储接缝从缺失到通过。`78-drafts-migration.log` **7/7 通过**（6762.9146ms），包括隔离、重读、过期覆盖拒绝、输入校验、v6 备份结构/项目保留及已有发送意图迁移回归。Main/Preload 增加 getDraft/saveDraft 两个有限接口，沿用主窗口、主 frame、参数个数检查；不开放路径读写。`79-draft-bridge-red.log` 确认旧窗口缺接口，`79-package.log` 新隔离包成功，`79-draft-bridge-green.log` **4/4 通过**（19252.5286ms），覆盖实际 SQLite 重开、精确桥白名单及迁移成功/失败路径。

## 30. 草稿输入框接线

useDraft 为每个项目/会话保存独立输入状态，读取期间禁止编辑，避免迟到读取覆盖输入；每个归属单独串行保存，写入期间的新输入在下一轮写入。失败保留本地内容并显式重试，不自动重新发送任务。首发成功后正常清除已发送草稿；首发期间继续输入的内容复制到新任务输入现场，避免自动进入任务时消失。补充失败仍保留原草稿。

`80-draft-ui-red.log` 和 `80-draft-ui-red-confirmed.log` 等待期间窗口关闭，只作为异常观察保留，不能据此证明功能缺失；改为直接观察后，`80-draft-ui-red-observed.log` 明确记录旧界面草稿状态缺失（0 ≠ 1）。`80-package.log` 隔离包成功，`80-draft-ui-green.log` **5/5 通过**（49364.4992ms）：实际草稿桥重开、分项目/任务恢复、首发期间编辑/导航，以及既有审批/补充/停止流程。`80-draft-failure.log` 保存失败专项 **1/1 通过**（6341.9965ms），通过真实窗口和真实存储验证显式重试保存最新编辑、不创建任务。

独立存储及 UI 审查未发现正常产品路径的确定性缺陷。数据库行归属字段遭外部篡改的额外完整性校验尚未覆盖，不能宣称覆盖所有损坏场景。上述自动测试均使用独立数据目录及合成故障，不读取正式凭据、不调用模型。最新 out 包及完整回归以 `81-out-package.log` / `81-full-tests.log` 结果为准。

## 31. 草稿版本全量回归与旧预期核对

`81-full-tests.log` 实际为 **105/108 通过**（350490.974ms），三项失败均保留原日志：关闭连接提示仍断言旧文案；取消目录选择用例仍要求草稿编辑后没有数据库；紧凑新会话仍要求复制旧会话草稿。按已批准的新功能更新明确预期，不放宽为泛化成功：检查关闭后密钥仍在、取消不创建项目/任务且草稿已落盘、新会话为空并能返回旧会话恢复原草稿。`84-regression-expectations.log` 对同一 out 包定向 **3/3 通过**（5130.8175ms）。不能将首次失败的全量日志改写为全部通过。

## 32. 真实计划事件

按固定 0.153.4 生成协议 `TurnPlanUpdatedNotification` / `TurnPlanStepStatus` 核对 `turn/plan/updated`，只接受 pending/inProgress/completed；没有事件时快照不含 plan，页面不生成计划。早到事件按 thread/turn 缓存，绑定后只显示当前轮，读取返回副本。匹配轮次结束或连接失效后不接受迟到计划，也不自动将尚未完成步骤改为完成。

`82-plan-red/green.log` 验证解析接缝；`83-plan-session-red.log` 确认缺少会话读取。首次 GREEN 尝试因测试替身误用 request 而非实际 call 方法失败，保留 `83-plan-session-green.log`，修正测试接口后 `83-plan-session-rechecked.log` **7/7 通过**（2008.015ms），覆盖现有执行协调和执行项回归。此处会话测试为协议调用边界替身，不是真实模型计划。

`85-plan-ui-red.log` 明确旧页面缺少实际事件中的计划步骤；新组件沿用既有执行项样式，提供折叠，非活动状态标注最后已知计划。`85-package.log` 隔离包成功，`85-plan-ui-green.log` 真实 Electron 专项通过（66245.9162ms），覆盖无事件不显示、收到合成事件后显示真实内容及既有控制流程。独立审查未发现计划串线或自动标完成问题。TypeScript 检查通过。本功能仍没有调用真实模型。

更新指定 out 包及新版完整回归的结果分别记录于 `86-out-package.log` / `86-full-tests.log`，不以此前结果代替。

## 33. 文件补丁修订事件

固定协议 `item/fileChange/patchUpdated` 更新同一文件执行项的 changes；修订提议不当作完成，已结束项不被迟到修订覆盖，其他 thread/turn 不得串入当前项。`87-file-patch-red.log` 记录缺失接缝，`87-file-patch-green.log` 与 `87-file-patch-integration.log` 分别通过解析与会话归属回归，后者 **4/4 通过**。调用边界使用替身，文件实际结果核对仍属于 M1-05，不宣称这里验证了真实文件副作用。

## 34. 新版打包与完整回归

`86-full-tests.log` 为 **108/110 通过**，包括草稿桥 45 秒超时以及紧凑菜单未出现；不覆盖或改写失败日志。在草稿启动、写入、关闭及重开处增加分段观测后，`88-timeout-observation.log` 定向 **2/2 通过**，草稿桥约 5.55 秒、菜单约 3.90 秒。未改超时阈值或放宽断言；因未再次复现，根因仍未确认，不将重跑通过称为根因修复。

`89-out-package.log` 已将补丁修订等最新功能打包至 `E:\AgentX\desktop\out\AgentX-win32-x64`。TypeScript 检查通过；同包 `89-full-tests.log` **112/112 通过**（149978.0027ms），包括两项先前失败用例。所有自动测试均使用隔离目录，不调用真实模型。

## 35. 输入框与停止按钮的视口约束

依据 PRD workbench 的底部固定输入组件约定，新增真实窗口几何断言，覆盖 1280×820 与 960×640。`90-composer-red.log` 明确复现紧凑窗口停止按钮底边 689 超出 640 高度（输入框底边 702）。执行工作台改为有界 flex 高度，只有时间线占用剩余空间并内部滚动，其他控件不压缩；没有新增浮动覆盖层或改动空白页布局。

`90-out-package.log` 已更新指定 out 包，`90-composer-green.log` **1/1 通过**（19732.2329ms），同时回归审批允许/拒绝、补充失败保留输入与停止非终态。已查看真实紧凑截图，输入框和停止按钮完整可见；多个状态提示同时存在时，时间线可见区域较短，仍需在整体工作台验收中检查阅读体验。此版 CSS 之后未再跑完整测试，不能将此前 112/112 当作新版全量结果。

## 36. 已提交要求留在会话中

执行快照从已有发送意图读取原始文本，核对 taskId 归属，仅投影 inputText，不暴露 credentialRef 或原始 RPC。尚无持久化意图时不伪造消息。工作台在时间线顶部展示已提交要求，后续草稿编辑不覆盖它；不以截断会话标题代替原文，也没有建立第二套引擎历史存储。

`91-input-red.log` 记录原快照缺失文本，`91-input-green.log` **3/3 通过**，覆盖实际协调/SQLite 链路在运行及中断后保留首次输入，以及无任务/准备失败不生成输入。`91-input-ui-red.log` 确认旧包缺少已提交区域；`91-out-package.log` 更新指定 out 包，`91-input-ui-green.log` **3/3 通过**（28632.3696ms），覆盖失败保留草稿、首发后原文与新草稿分别显示、切换项目不被迟到应答导航及既有执行控制。TypeScript 检查通过。桌面执行边界仍为明确的合成替身，没有调用模型。

## 37. 首发键盘入口

按 PRD UI-COMPOSER，Ctrl+Enter 在新任务中复用 sendFirstTurn 的校验与同步提交锁，在活动轮中仍调用补充要求；普通 Enter 不拦截，IME 组合事件不发送。没有创建第二套发送路径，也不将快捷键变为停止操作。

`93-first-send-keyboard-red.log` 确认旧包按快捷键后没有提交。`93-out-package.log` 已更新指定 out 包，TypeScript 检查通过；`93-first-send-keyboard-green.log` **3/3 通过**（19876.3972ms），验证 IME 组合不增加请求、快捷键正确提交一次及运行控制回归。

`92-full-tests.log` 为 **110/112 通过**（392624.6701ms）：执行期间修改了随后才被测试运行器加载的首发用例，使旧包面对新增快捷键要求，两项均在该断言超时。因此该轮不是冻结版本的一致性全量证据，不能声称已完成新版全量验证；保留原日志。后续完整回归期间不再修改测试或覆盖其应用包，桌面专项串行运行。

## 38. 冻结版本回归和手动真实验收入口

`94-frozen-full-tests.log` 在不修改测试和产品代码、不覆盖应用包的条件下 **112/112 通过**（172316.7203ms）。这一结果对应 93 包，覆盖已提交原文、首发快捷键以及有界输入布局。

新增 `scripts/m1-live-smoke.cjs` 作为手动真实产品首轮验收入口，不加入 npm test；只有显式 `--live` 和进程环境中的 `AGENTX_LIVE_DEEPSEEK_KEY` 同时提供才继续。仅选 `deepseek-v4-flash`、最多一次发送、不自动批准或重试，使用独立数据和合成项目，真实执行/模型/存储接口不替换。只有目录对话框由验收脚本提供合成项目选择。

先独立确认合成测试失败，完成后重跑原测试并核对测试源码和人工文件未被修改。记录实际任务终态和工具种类，不导出凭据或原始异常。语法与无 --live 拦截已检查（`95-live-opt-in-guard.log`）；尚未注入真实密钥或运行真实首轮，脚本存在不等于真实验收完成，也不代替审批允许/拒绝、补充、中断和后续切片验收。

## 39. 首次真实 Flash 产品执行通过

本地密码窗口启动未成功（`96-live-smoke.log`，未调用模型）；用户随后提供本次密钥，通过进程环境交给验收脚本，脚本立即移除环境项，并由产品加密存入独立数据目录。没有把密钥写进脚本、Git 或验收日志。

`97-live-smoke.log` 对应运行 `a0764618-aad2-4429-9aa5-4e83b1f36835`，使用 93 版真实打包 Electron，**仅一次首发、仅 deepseek-v4-flash**。执行、模型、存储和审批接口没有替换；只为原生目录选择对话框提供了合成路径。产品最终报告 completed，收到了 message、command、fileChange 三类执行项，并报告至少一个退出码为 0 的命令。

实际 `math.cjs` 从减法修复为加法，`text.cjs` 修复为 `Hello, ${name}!`。先前失败的合成测试独立复跑 **2/2 通过**，测试源码和 `human-notes.txt` 均逐字保持不变。脚本摘要 `passed: true`；主代理又通过独立 Node 进程重跑 **2/2 通过**（87.1451ms）。已检查真实工作台截图。

本次观察到了原生审批请求，脚本没有自动答复；当前脱敏记录没有保存具体审批决定及其绑定证据，因此不能把 approvalsObserved 当作“允许/拒绝分支均验收通过”。最终正文提到了沙箱内测试失败后再执行的情况；仅凭正文不归因，审批范围和实际命令证据仍需单独核对。补充、明确拒绝、运行中停止及断线不重发仍待真实验证。

原始隔离运行目录仅保留本地（含系统加密凭据，禁止上传）：`.local-validation/m1-live/a0764618-aad2-4429-9aa5-4e83b1f36835/`。只有经检查的脱敏摘要、截图可作为 PR 证据。此记录证明首轮闭环，不意味着 M1-04 或完整 M1 已完成。

## 40. 真实停止副作用失败与接口边界

`98-live-stop.log` 对应 `de5b1088-8b92-4cd0-909f-8b0b7b82dd1c`，只发送一次 Flash 任务；先观察到 delayed.cjs 创建 started.txt，再通过真实工作台停止按钮中断。产品状态实际经过 submitting、running、interrupted，但延迟 60 秒仍生成 finished.txt。因此本项 **失败**，不能把 interrupted 或按钮变化当作工具全部停止。脚本没有重试模型。与上一节合计已使用两次真实顶层发送。

已核对固定 0.153.4 生成协议：稳定 `turn/interrupt` 只负责中断轮次；`command/exec/terminate` 针对客户端独立 command/exec，不可据此终止 Agent 内部命令。`thread/backgroundTerminals/list` 和 `thread/backgroundTerminals/terminate` 仅出现在 experimental 协议快照，需要 threadId/processId。Context7 检索到官方 app-server README 同样明确：turn/interrupt 不终止后台终端，显式清理须使用后台终端接口。

建议以产品适配器管理属于当前任务的后台终端：固定版本下列出、按归属终止并重新核对；失败保持核对状态，不伪装完全停止。不修改 Codex、不杀用户全局进程、不重发任务。该方案需要将最小实验性接口加入已固定的协议面并设置 experimentalApi，属于已知协议边界变化；在获得确认前不修改产品握手或停止语义，先保留失败事实。

## 41. 已确认停止合同与适配器接缝

用户已明确确认停止包含“轮次中断和后台终端终止”，不要求进程冻结/原地恢复。按此前限定方案接入固定版本最小实验性接口，产品仍保持零 Codex 补丁。只处理当前任务归属的终端，不清空无关后台资源。

新增 Main 内部 terminateBackgroundTerminals：分页读取，按已知执行项归属终止，之后重新查询；重复关联、异常分页、无效应答及仍在运行均显式失败。terminated=false 不能直接断定失败或成功，仍以列表核对。`99-background-red.log` 确认缺失接缝，`99-background-green.log` **5/5 通过**（347.504ms），TypeScript 检查通过。这是协议边界替身测试，尚未接到产品停止协调或启用实验性握手，也未重跑真实模型。完整功能尚未完成，当前 out 包的停止能力仍是第 40 节已知失败版本，不宣称已修复。

## 42. 双重停止协调与真实副作用复验

握手按已批准范围启用 experimentalApi，仅新增两个后台终端适配调用，不开启额外模型工具或修改 Codex。停止时先持久化 stopping，再发送 turn/interrupt；匹配终态暂存，不立即落终态。随后按命令 itemId 归属查询、终止并复查后台终端，核对通过才落原轮次终态；无终态、断线、清理失败均保留核对状态。重复停止仍被持久化状态拒绝。

`100-stop-coordination-red.log` 复现旧实现收到终态即提前结束；对应 GREEN 通过。`100-stop-regression.log` **19/19 通过**（13339.4287ms），包含握手、协议、审批、停止协调与发送存储。`100-out-package.log` 更新用户指定 out 包，桌面控制专项 `100-stop-desktop.log` **1/1 通过**（25646.0825ms）。独立审查确认清理失败不落成功终态；提出的路径大小写兼容点尚未扩大修改。

`101-live-stop.log` 对应真实运行 `3a799d86-0ed1-46e9-90c6-e4f4e41fc7f0`，仅一次 Flash 发送。命令创建 started.txt 后，验收脚本点击真实工作台停止按钮，等待 interrupted，并继续观察至停止请求后 65 秒。delayed.cjs 与测试源码未变，finished.txt 未出现；脚本通过，主代理再次独立检查也确认 started 存在、finished 不存在。本项真实副作用复验 **通过**，第 40 节失败日志保留不改写。本批目前共三次真实顶层发送，没有扩大模型范围。

本次采样记录 running/interrupted，未采到短暂 stopping；不能用采样宣称其可见持续时长。stopping 期间不能提前终态由确定性测试证明。此单项成功不等于覆盖任意逃逸/外部服务进程；未关联终端不擅自杀掉。

新版冻结回归 `102-frozen-full-tests.log` **114/114 通过**（173661.1219ms）；运行期间未修改产品代码、测试或覆盖 out 包，不调用真实模型。

## 43. 真实原生审批拒绝

`103-live-deny.log` 对应 `35e07c7c-e6b2-4e8f-a975-c754d1f2ad24`，仅一次 Flash 发送，目标为本次独立验收目录中、项目目录之外的 denied.txt，不涉及真实业务目录。模型实际发起原生审批，验收脚本通过真实工作台对应卡片点击拒绝，没有替换执行/审批 IPC。产品收到该审批 resolved，轮次 completed，目标文件未创建；主代理再次独立检查确认不存在。测试源码和人工文件保持不变。本项 **通过**，累计四次真实顶层发送。

拒绝只代表这次操作被拒绝，不表示整个任务必须进入 failed，也不回滚此前工作。这个用例验证的是审批拒绝及目标写入未发生，不代替允许审批、补充或历史验收。产品包没有因验收脚本变化而更改，仍为已全量通过的 100 包。

## 44. 真实运行中补充要求

`104-live-steer.log` 对应 `f5377f6c-790c-44b6-b6f2-b9126f73104a`，仅一次 Flash 顶层发送。实际命令创建 started.txt 后，通过工作台输入框提交补充要求，页面显示“补充要求已接收”；轮次完成时 taskId/threadId/turnId 均与补充前一致。实际 supplement.txt 内容为 STEER_ACCEPTED，原 delayed.cjs、测试源码和人工文件保持不变，并有成功命令退出码。脚本验收 **通过**，主代理独立读取文件再次确认。

本批累计五次真实顶层发送，达到本批上限，后续不自动发起第六次。此项证明补充在同轮次接收并产生结果；不等于中断后续轮或重开历史恢复，也没有证明所有原生审批允许分支。本阶段继续无密钥审查与工单证据整理，额外真实验收须另行明确触发。

## 45. 停止清理失败的产品接缝回归

新增 `execution-service.test.cjs` 用例，通过真实 ExecutionService、stdio 传输解析与 SQLite 链路注入“终止应答成功、复查仍存在”的后台命令状态。断言轮次中断事件不能提前结束 stopping；停止请求明确失败，产品及持久化记录进入 reconciling；原始输入保留，重复停止和新任务被拒绝，原 turn/start 与 turn/interrupt 均只发送一次。列表中的其他执行项不被终止。

`105-cleanup-failure.log` **4/4 通过**（1567.7648ms）。这是给已有行为补充的特征回归，首次运行即通过，不伪造 RED，不新增产品实现。替身只在引擎进程边界注入确定性错误，没有调用模型；不等于真实引擎故障现场验收。产品代码和 out 包未变化，仍为第 42 节记录的 100 包；新的全量测试总数尚未重新统计。

当前边界：若 turn/interrupt 失败或 30 秒内没有匹配的轮次终态，进入 reconciling，不会进入后续后台终端清理。这不表示后台命令已经停止，也不能提供“所有进程立即暂停”的承诺。是否在无法确认轮次结束时额外尝试清理，须同时解决活动轮继续产生新命令的竞态，不能用一次列表清理冒充完全停止。此处保留已批准的失败可见、禁止重发合同，不擅自改为杀引擎或杀全局进程。

独立审查指出的“审批允许一次未记录真实决定”“人工缩放与输入法检查未逐项留证”属于验收证据缺口，不能仅据缺少现场记录断定实现有缺陷。断线及旧审批路径的确定性测试需要与现场证据分开呈现；自动测试不能冒充真实模型副作用验收。

## 46. 待核对与旧审批的真实桌面状态验证

`tests/desktop/workbench-execution.test.cjs` 新增独立场景，启动当前打包 Electron，使用隔离数据。执行快照 IPC 为显式替身：先显示等待审批，随后注入断线、reconciling 和 staleApproval；Renderer、Preload、窗口、草稿读写均为产品实现。所有执行控制入口都有计数探针，页面进入设置再返回、输入框 Ctrl+Enter 后仍未发送 start/steer/stop/approval。

`106-reconciling-desktop.log` **1/1 通过**（3254.5241ms）。确认旧审批允许/拒绝按钮禁用、错误可见、中文草稿保留，离开页面不隐式批准。截图 `106-reconciling-stale.png` 已实际检查：状态待核对、失效审批、红色错误及保留的草稿同时可见。此次不是中文输入法候选窗专项，也不是真实 Codex 断线或真实模型验收；产品包未变。

## 47. 手动验收密钥弹窗的编码问题

复核第 96 次启动失败时，在 Windows PowerShell 对 `m1-live-key-dialog.ps1` 执行 ParseFile，复现字符串终止符缺失及后续语法错误；文件本身为约定的 UTF-8 无 BOM。改为显式 UTF-8 读取再构造 ScriptBlock，以 EncodedCommand 传递只有本地脚本路径的加载表达式，不把密钥加入命令行，也不改变脚本编码。

`107-dialog-parse.log` 为 UTF8_PARSE_PASS，Node 脚本语法检查通过。自动打开并取消弹窗的尝试未得到有效结果：命令会话终止，stdout/stderr 为空，随后查询无该 EncodedCommand 测试进程；不能写为取消测试通过。实际密钥输入、取消交互仍待复核。本次没有提供密钥或调用模型，只修改手动验收加载器，不改变用户 out 产品包。

第 46 节之后两个受影响测试文件的合并回归 `106-control-regression.log` **6/6 通过**（9538.3389ms）。本批真实调用次数仍为五次，下一批尚待用户明确触发。

## 48. 草稿审查前冻结回归

`108-frozen-full-tests.log` **116/116 通过**（153069.0079ms），覆盖现有单元、协议集成和真实 Electron 桌面测试。运行期间未修改测试或产品代码，也未覆盖 out 包。使用独立数据和合成凭据，不调用模型。远程 Issue #9 再次核对仍为 OPEN、enhancement + ready-for-agent。

准备提交可审查的 M1-04 基线与草稿 PR，不表示本切片已验收或允许依赖它的切片开始。真实审批允许一次、尚未逐项完成的人工专项与最终现场验收继续作为未完成项；草稿 PR 不关闭 Issue、不合并 m1 或 master。

## 49. 尚未完成

当前已接通固定资源、stdio、模型快照、首次发送、侧栏实时投影、配置待生效提示、草稿分任务持久化及工作台基础执行/审批/补充/停止接口，并通过首次真实 Flash 文件修复。仍缺完整必要事件覆盖和其余真实控制副作用验收；退出时未完成的草稿写入也需纳入 M1-06 存活与退出检查。不能据确定性替身用例声称完整执行闭环已通过。Issue #9 不关闭，不合并未完成切片。后续按工单 States 矩阵逐行为推进。

除第 39、40、42、43、44 节明确记录的真实调用外，前述自动测试不调用模型；所有检查均不操作真实业务项目。所有真实接入、测试会话和最终验收只允许 `deepseek-v4-flash`；其他拉取模型不得调用。M1-05/M1-06 与最终任务闭环仍未完成，`master` 不变。
