# AgentX V1 视觉基线

确认日期：2026-09-07。用户已确认“V1 版本先这样”，随后批准进入最小兼容验证。

本目录固定默认视觉参考，不代表对应产品代码已实现或验收通过。功能与状态仍以 [PRD](../../prd/AgentX_Desktop_PRD.md) 为准。

| 规格 | 默认参考 | 来源 |
| --- | --- | --- |
| app-shell | [默认外框](windows-desktop-app-shell.png) | v0.3 的 01 新会话图 |
| workbench | [默认工作台](windows-desktop-workbench.png) | v0.3 的 01 新会话图；与外框共用构图 |
| settings | [默认设置](windows-desktop-settings.png) | v0.3 的 20 通用设置图 |

其余状态采用 [v0.3 图册](../review/v0.3/gallery.md) 中的 25 张采用稿；带 corrected 的版本优先，修订前稿仅留档。两个功能界面加一个公共壳，不新增 25 个页面。

本次仅复制已有 PNG，不重新生成或改变布局。正式实现仍需真实交互、键盘、缩放及状态验证。
